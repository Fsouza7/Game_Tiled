# Roadmap

Phases are built and validated in order; a phase is not started until the
previous one is playable and tested. See `README.md` for how to run the
game and its tests.

## Phase 1 — Playable prototype: DONE

- [x] Tile-based world with chunking (lazy load/unload)
- [x] Deterministic seeded generation: sky, dirt, stone, coal/iron ore, caves, bedrock floor
- [x] Player movement, gravity, tile collision, fall damage, respawn
- [x] Camera: smooth follow, world-bound clamping, zoom
- [x] Mining (tool-gated, timed) and building (placement requires adjacency)
- [x] Inventory: slots, stacking, hotbar, add/remove
- [x] Headless automated tests for all of the above

Known gaps, intentionally deferred (not fake/stubbed — simply not built yet):
- No save/load (closing the game discards the world)
- No lighting/day-night, crafting, combat, biomes, NPCs, bosses
- No real art (flat-colored placeholder tiles/player)

## Phase 2 — Data-driven item system: DONE
- [x] Expanded `ItemDef`: description, category, icon_key, rarity, value, damage, speed, max_durability
- [x] All 8 categories represented in `item_registry.py`: block, ore, material, tool, weapon, consumable, armor, decoration
- [x] Decoration category is fully functional end to end (wooden_crate: rare surface spawn -> mine -> place), not just data
- [x] Headless tests: category coverage, schema fields, crate spawn/mine/place

Known gap, intentionally deferred: material/weapon/armor/consumable items
(wood, wood_sword, wood_helmet, apple) exist as complete data but have no
obtain path yet (no trees, no crafting, no drops) and no usage mechanic
(no equip/attack/eat). They become obtainable and usable as Phase 3, 4 and
the equipment pass (below) land.

## Phase 3 — Crafting: DONE
- [x] Recipe schema (ingredients, quantities, result) + registry (`game/crafting/`)
- [x] Crafting stations: workbench tile, station-proximity gating (`STATION_SEARCH_RADIUS_TILES`)
- [x] Crafting UI (`C` key): shows every recipe, live ingredient counts, green/red ready state
- [x] Real trees (3-tile trunk + 3-wide canopy, deterministic slot-based placement) so wood is gathered from trees, not a floating log tile
- [x] 4 recipes: workbench, wood plank (building block), wood pickaxe (remake), stone pickaxe
- [x] Fixed item icons that didn't match their item (ore/wood were flat color swatches) -- they now borrow their tile's texture; tools/weapon/armor got hand-drawn vector icons instead of anonymous swatches
- [x] Tile visuals improved: ore got speckle details, workbench/trunk/planks got procedural bark/plank/tabletop details instead of a flat tint
- [x] Headless tests: registry validation, ingredient/station gating, craft transaction, refund-on-full-inventory, tree spawn/canopy shape, icon correctness

Known gap, intentionally deferred: only 4 recipes exist -- enough to prove
the system end to end. More will be added as later phases introduce things
worth crafting (weapons in Phase 4, armor in Phase 9).

## Phase 4 — Combat: DONE
- [x] Melee attack (hitbox in front of the player) and ranged attack (aimed projectile) via a weapon selected in the hotbar
- [x] Projectiles: straight-line + light gravity arc, destroyed on hitting a solid tile or an enemy, timed lifetime
- [x] Knockback (both directions) and temporary invulnerability (player + enemy i-frames)
- [x] Death and drops: enemies drop loot (rolled from `EnemyDef`) directly to the player's inventory on death
- [x] 3 enemies with distinct AI: Slime (hop), Crawler (ground patrol/chase, avoids walls and ledges), Duskwing (flying hover/chase)
- [x] Spawner: periodic, weighted by `spawn_weight`, capped at `ENEMY_MAX_ALIVE`, despawns enemies that end up far from the player
- [x] New craftable weapons/ammo so combat is actually playable: Wood Sword, Wood Bow, Arrow (all at a workbench except arrows)
- [x] Refactored Player's tile collision into `entities/tile_collision.py` so Enemy reuses it instead of duplicating it
- [x] Headless tests: AI collision probes, ledge/wall avoidance, melee/ranged damage, projectile lifecycle, contact damage/i-frames, spawner cap/despawn
- [x] Aiming polish: melee is a cone toward the mouse (not just left/right), a swing-arc visual, continuous pre-aim facing while a weapon is out, and an aim reticle
- [x] Sword balance pass (user playtest feedback): reach shortened from a spear-like 1.4 to a punchy 0.9 tiles, attack rate raised from 1.2/s to 3.0/s so swings feel responsive rather than sluggish
- [x] Duskwing balance pass (was "almost impossible to kill without taking a hit"): contact damage 10 -> 7, speed 6.5 -> 5.5, and it now backs off once within `FLYING_STANDOFF_TILES` instead of camping inside the player's hitbox dealing free contact damage every frame
- [x] Melee hit-detection generosity pass (user playtest feedback: small enemies were hard to actually connect with): reach nudged back up to 1.1 tiles, swing cone widened 110 -> 130 degrees, and small enemies (slime, duskwing) are now treated as at least `MELEE_MIN_TARGET_SIZE_TILES` (1 tile) for hit-detection purposes only -- their real collision box, movement and rendering are unchanged
- [x] Second reach increase (user playtest feedback: 1.1 tiles still felt too short): `MELEE_REACH_TILES` raised to 1.8 -- swings now reliably connect out to ~2.5 tiles from the player (measured), up from ~1.7-2

Known gaps, intentionally deferred:
- No physical dropped-item entities (loot goes straight to inventory)
- No enemy ranged attacks -- all three current enemies only deal contact damage
- `assets/20 Enemies.png` turned out to be a promotional preview image (watermarked "download all 20 enemies"), not usable art, so enemies are flat colored shapes until real sprites are added

## Equipment & UI pass (user-requested, ahead of the formal Phase 9): DONE
- [x] Real RPG-style equipment: `game/inventory/equipment.py` adds Head/Body slots; equipping moves the item out of the bag, unequipping moves it back (with a full-inventory refund guard, same pattern as crafting)
- [x] Equipped armor has a real effect: `defense` is subtracted from contact damage in combat, floored at `MIN_DAMAGE_AFTER_DEFENSE` (not just a cosmetic slot)
- [x] Two new craftable armor pieces so the equip screen isn't permanently half-empty: Wood Helmet (head, craft anywhere) and Wood Armor (body, requires a workbench) -- `leather_cap` was renamed to `wood_helmet` for thematic/tier consistency with the rest of the wood-based early game
- [x] Inventory screen redesigned as a two-panel character sheet: portrait + equipment slots + HP/Defense/Attack stats on the left, the bag grid on the right; every occupied slot (bag or equipment) is now bordered by `ItemDef.rarity`
- [x] Crafting screen redesigned: sectioned into "Craft Anywhere" / "Requires a Workbench", ingredient icons instead of text-only, hover highlighting, a ready/not-ready accent bar, and mouse-wheel scrolling (`CRAFTING_VIEWPORT_HEIGHT`) so the panel can't grow into the HUD as more recipes are added
- [x] Headless tests: equip/unequip transactions (including the full-inventory refund case), defense reducing combat damage, recipe/item registry sanity, UI slot-rect geometry (no overlaps, click hit-testing matches what's drawn), section grouping, scroll clamping

Known gaps, intentionally deferred:
- Still no drag-and-drop; equip/unequip/craft are click-to-act, not click-and-drag
- The hotbar-selected weapon is *not* a separate "equipped weapon" slot in the character sheet -- it's just the current hotbar selection, mirrored as an "Attack" stat
- Legs/Boots slots and Iron/Steel armor sets later landed in Phase 9

## Health regen (user-requested): DONE
- [x] Passive regen (`PLAYER_REGEN_RATE_HP_PER_S`) while below max health, paused for `PLAYER_REGEN_DELAY_AFTER_DAMAGE_S` after any damage (contact or fall) so it can't out-heal an active fight
- [x] `Player.is_regenerating()` drives a pulsing health-bar border; F3 debug overlay shows the countdown while paused
- [x] Headless tests: heals at the correct rate, capped at max health, paused immediately after damage, resumes exactly when the delay clears, re-paused if hit again mid-regen, cleared on respawn

Known gap at the time, since resolved by the "Asset pass" below: no
food/potion healing -- `apple` was catalog-only (no "eat" action existed).

## Phase 5 — World cycle: DONE
- [x] `WorldClock` (`game/core/world_clock.py`): a real-time day length (`DAY_LENGTH_S`), a cosine ambient-light curve (no discrete day/night jump), `is_night`, and an HH:MM clock string
- [x] Sky tint follows the same ambient curve: a navy overlay peaking at midnight, a warm overlay peaking at dawn/dusk, both derived from one value so they can't drift apart
- [x] Tile-based point-light lighting (`game/world/lighting.py`): brightness = max(ambient, falloff from any nearby `TileDef.light_emit` source), searched only within the visible viewport + a margin -- never the whole world (~10ms/frame end to end with lighting + day/night active, measured)
- [x] Torch: new tile + item + recipe (wood + coal -> 3 torches, craftable anywhere), `light_emit=14`, non-solid, with a procedural stick+flame texture and a subtle flicker
- [x] Night-only enemy spawning: `SpawnTime` (ANY/DAY/NIGHT) on `EnemyDef`; Duskwing is now `NIGHT`-only (fitting, given its name) -- `EnemySpawner` filters eligible enemies by `WorldClock.is_night` before the weighted pick
- [x] Day/night HUD indicator (sun/moon icon + "Day N HH:MM"); F3 debug overlay shows day count, clock, day/night, and the raw ambient value
- [x] Fixed a rendering bug found while building this: darkening the player sprite with a plain rectangular overlay showed up as a visible box around its transparent background; switched to an RGB-only multiply blend that preserves the sprite's own alpha (enemies, being flat vector shapes, just get their fill color dimmed directly -- no such issue there)
- [x] Headless tests: clock wraparound/day-count/smoothness/is_night threshold, light falloff/radius/bounds/viewport-only search, torch tile/item/recipe, night-exclusive spawn filtering

Known gaps, intentionally deferred:
- Lighting doesn't respect occlusion (no flood-fill/shadow-casting) -- a real simplification, documented in README "How the day/night cycle and lighting work"
- Only 1 light source (Torch) and only 1 night-exclusive enemy (Duskwing) exist so far -- more of each are trivial to add via the existing data-driven registries
- Caves/underground areas use the same ambient curve as the surface (no permanent underground darkness independent of time of day) -- would need a "sky exposure" concept per column, not built yet

## Phase 6 — Biomes: DONE
- [x] Fixed-width horizontal zones (`BIOME_ZONE_WIDTH_TILES`), each independently rolling a biome via seeded weighted choice (`biome_registry.BiomeDef.zone_weight`); a fixed-radius safety window around the exact world center is always Forest so spawn is never stranded in Desert/Snow/Jungle
- [x] 4 surface biomes with their own surface/subsurface tiles: Forest (grass/dirt, unchanged), Desert (sand/sandstone), Snow (snow block/frozen dirt), Jungle (jungle grass/mud)
- [x] Per-biome deep underground: each biome has its own stone filler (`BiomeDef.underground_tile_id` -- Forest keeps plain Stone, Desert/Snow/Jungle get Desert Stone/Permafrost Stone/Jungle Stone) plus a small extra roll at a biome-exclusive gem ore (`exclusive_ore_tile_id`/`exclusive_ore_chance` -- Topaz/Sapphire/Emerald), stacked on top of the universal Coal/Iron find that still exists under every biome; caves stay identical everywhere (same noise field regardless of which biome's stone they carve out of)
- [x] Desert-exclusive vegetation: normal tree roll is replaced by cacti (1-2 tiles tall); Snow and Jungle currently reuse Forest's tree system unchanged
- [x] New material: `cactus_fiber` (mined from cacti) plus a new recipe, `arrow_cactus` (2 fiber -> 5 arrows), so desert exploration has its own resource loop
- [x] Biome-gated enemy spawning: `EnemyDef.biome_id` (None = any biome); Scorpion is the first biome-exclusive enemy (Desert only) -- `EnemySpawner` now resolves the biome at the candidate spawn column before the weighted enemy pick
- [x] F3 debug overlay shows the current biome name
- [x] Headless tests (`tests/test_phase6_biomes.py`, 24 tests): zoning determinism/seed-variation/full coverage, per-biome surface tile columns, per-biome underground stone/exclusive-gem coverage + mining one, desert no-trees/cactus-spawn/mine-for-fiber, item/recipe registry sanity, Scorpion biome gating both ways

Known gaps, intentionally deferred:
- Only 1 biome-exclusive enemy (Scorpion/Desert) -- Snow and Jungle have no exclusive enemy yet
- Hard biome borders -- zones change abruptly at their boundary, no blending/transition strip
- Caves are identical in every biome (no biome-specific cave generation, only the stone/ore they're carved out of differs)
- The three biome-exclusive gems (Topaz/Sapphire/Emerald) later gained smelt recipes and, in Phase 9, combine into an Arcane Bar
- Snow and Jungle have no unique surface vegetation/decoration of their own yet -- they reuse Forest's tree system as-is
- "Cave" isn't a zoned biome (see README "How biomes work") -- it's the Phase 1 universal underground, unaffected by the surface biome above it

## Asset pass (user-requested: survey every file in assets/ and put unused ones to work): DONE
- [x] Food + a real "eat" action: 8 items (`apple` plus 7 new fruits from `assets/Items/Fruits/`) get `ItemDef.heal_amount`; `Player.eat_selected()` (bound to `F`) heals and consumes one, no-op at full health or on non-food
- [x] Obtain path for food: Berry Bush (`BUSH_ID`), a new rare surface spawn in Forest/Snow/Jungle (shares Desert-exempt slot with the wooden crate) that drops one random fruit -- the first tile to need "one of several possible drops," via a new minimal `TileDef.drop_pool` field alongside the existing single-item `drop_item_id`
- [x] Two placeable interactive tiles from `assets/Traps/`, each via one new opt-in `TileDef` field so every existing tile is unaffected: Spikes (`contact_damage`, checked every frame the player overlaps a tile via `combat_system.resolve_hazard_damage`, mirroring the existing enemy contact-damage path/invulnerability window) and Trampoline (`bounce_velocity`, checked by `Player.physics_step` on landing -- launches up and skips fall damage instead of coming to rest)
- [x] New recipes: `spikes` (5 stone + 2 wood) and `trampoline` (6 wood + 3 slime_gel), both at a workbench
- [x] Headless tests (`tests/test_food_and_hazards.py`, 13 tests): eat heals/consumes/caps/no-ops (full health, non-food), bush spawn/biome-exclusion/mine-for-random-fruit, spikes damage + invulnerability window, trampoline bounce-instead-of-rest

Assets surveyed but deliberately not used this pass (see README "Known
limitations" for what stays open): the other 3 platformer characters
(MaskDude/PinkMan/VirtualGuy -- no character-select mechanic exists),
`20 Enemies.png` (a watermarked promotional image, not usable art, already
noted in Phase 4), Menu/Buttons (the pause screen is still a plain overlay;
wiring real buttons to actions like Settings would need those actions to
exist first), Checkpoints (no natural fit yet -- respawn is a fixed point,
not a placeable/activatable one), and the remaining Traps (Fan, Saw, Rock
Head, Spike Head, Falling Platforms, Sand/Mud/Ice) and Other/ particles
(Dust, Shadow, Confetti) -- each would need a new subsystem (animated
hazards, a particle system) rather than one new TileDef field, so they're
left for a future pass instead of half-implemented here.

All of the above, since implemented in "Deferred-assets pass 2" below
(user-requested: everything on this list, done properly rather than
half-implemented) -- except `20 Enemies.png`, which stays unused for the
same reason it always was (not usable art).

## Deferred-assets pass 2 (user-requested: implement everything deliberately left out of the first Asset pass): DONE
- [x] Playable character select: `game/entities/character_registry.py` registers all 4 interchangeable skins in `assets/MainCharacters/` (Ninja Frog, Mask Dude, Pink Man, Virtual Guy -- same frame size, same animation filenames); a one-time screen at startup (`GameApp.character_select_open`) lets the player click a portrait or arrow-key between them and confirm with Enter, then plays exactly as before with that skin -- `Player.character_id` is the only new field, Renderer preloads every character's animations up front and looks up the right set by id
- [x] Real pause menu: the pause overlay now has 4 working buttons (`assets/Menu/Buttons/Menu_jogo/`) instead of just a "PAUSED" label -- Resume, Settings (Zoom In/Out, Back -- real, already-existing camera controls, not fake placeholders), Restart (rebuilds the whole run from the same seed via `GameApp.restart()`/`_new_run`, kept skin), and Quit
- [x] Checkpoints (`game/world/checkpoints.py`, `tile_registry.CHECKPOINT_ID`): a craftable, placeable tile; touching one (exact-tile overlap, like Spikes) makes it the player's new respawn point and fires a Confetti burst -- unit-tested via `checkpoints.try_activate` without needing a full GameApp
- [x] A small generic particle system (`game/rendering/particles.py`): Dust (footstep puffs while running on ground) and Confetti (one-shot burst on checkpoint activation), both real source art from `assets/Other/`. The ever-present ground Shadow from the same folder is deliberately *not* a particle -- it's a persistent decal drawn directly under the player/enemies each frame, not a spawn/expire effect
- [x] Fan (`tile_registry.FAN_ID`, new opt-in `TileDef.updraft_velocity`): a placeable tile that pushes the player upward at a steady velocity while within `FAN_RANGE_TILES` above it, tapering to 0 at max range -- same "one new opt-in TileDef field" pattern as Spikes/Trampoline
- [x] Sand/Mud/Ice traps (`TRAP_SAND_ID`/`TRAP_MUD_ID`/`TRAP_ICE_ID`, new opt-in `TileDef.speed_multiplier`): placeable tiles that slow (Sand/Mud) or speed up (Ice) the player's horizontal movement while standing on them, applied in `Player.physics_step`'s ground-only horizontal move
- [x] Falling Platform (`CRUMBLE_PLATFORM_ID`): a placeable tile that crumbles to air after `FALLING_PLATFORM_TRIGGER_S` of continuous standing (any other tile, or leaving it, resets the timer) and reforms after `FALLING_PLATFORM_RESPAWN_S` -- state lives in `World` (`notify_standing_on`/`update`), not the Player, so it's independently testable
- [x] Moving hazards -- Saw, Rock Head, Spike Head (`game/world/hazard_feature.py`): each is anchored to a fixed underground position rolled deterministically at chunk-generation time (same "pure function of seed+column" guarantee as trees/ore, see `world_generator.generate_hazard_anchor`), and its current position is a sine oscillation of elapsed world time -- no per-frame mutable state, survives chunk unload/reload identically. This is a deliberate simplification of the source material (a "real" Rock Head/Spike Head charges once triggered by player proximity); these instead swing/thump on a fixed rhythm the player has to time a pass around, avoiding the need for a trigger/charge/return state machine colliding against the static tile grid
- [x] Headless tests (`tests/test_deferred_assets_pass2.py`, 25 tests): character registry/asset coverage, Fan updraft in/out of range, Sand/Mud/Ice speed multipliers (and that they don't apply mid-air), Falling Platform crumble/respawn/timer-reset, Checkpoint activation/re-activation/respawn, hazard anchor spawn coverage + oscillation math + contact damage/knockback/invulnerability, particle expiry/confetti count, new item/recipe registrations
- [x] Double jump (user follow-up: another previously-unused sprite, `double_jump.png`, exists per character): `Player.jump_count`/`PLAYER_MAX_JUMPS` allow one extra mid-air jump, reset on landing/respawn; the "double_jump" animation plays for `PLAYER_DOUBLE_JUMP_VISUAL_DURATION_S` after the *second* jump specifically (not the first), then falls back to the normal jump/fall pose -- 3 headless tests in `tests/test_phase1_smoke.py` (second jump works airborne, a third jump is a no-op, jump_count resets on landing)

Known gaps, intentionally deferred:
- Saw/Rock Head/Spike Head swing on a fixed rhythm rather than charging when the player gets close -- see the simplification note above
- The Sand/Mud/Ice traps reuse the same tinted-base-tile technique as the biome ground tiles they resemble rather than slicing `assets/Traps/Sand Mud Ice (16x6).png`, whose cell layout isn't documented anywhere in the pack (same call already made for every biome tile with no unambiguous single-cell source art)
- Fan renders as a static tile (first frame of its "on" animation) -- like Spikes/Trampoline, there's no per-tile animation state in the plain id->texture cache
- No drag-and-drop or free camera placement for hazards; Saw/Rock Head/Spike Head are environment features from world generation, not player-placeable items
- Settings originally only exposed Zoom; a later "Settings screen overhaul" pass below added Music/SFX Volume once an audio system existed

## Crafting UX pass (user-requested: better layout, recipe discovery, a blocked-mining message, a Furnace): DONE
- [x] Crafting screen redesigned again: primary grouping is now by the result item's **category** (Building/Structures & Utility/Tools/Weapons/Armor/Ammo/Materials), a layout a player recognizes at a glance, instead of only by station; each row still carries its own station requirement as a small badge (`_station_label`) so "where do I make this" is still one glance away
- [x] Recipe discovery/unlock system: a recipe is hidden behind a "???"-style locked row until the player has *ever* obtained (mined/looted/crafted/smelted) every one of its ingredients at least once (`Player.discovered_item_ids`, permanent -- not cleared by respawn, and not the same as currently holding the item). Locked rows are specific, not mysterious: they name exactly which ingredient(s) are still undiscovered (`crafting_system.undiscovered_ingredients`) rather than hiding the recipe's name entirely. Obtaining the last missing ingredient pushes a "New recipe unlocked: X" toast (see notifications below)
- [x] On-screen notifications (`game/core/notifications.py`, a small FIFO toast queue at top-center): recipe-unlock announcements, furnace outputs ready, and -- the specifically-requested case -- trying to mine a tool-gated block without the right tool now shows "Requires a Pickaxe" instead of silently doing nothing (`Player.blocked_mining_reason`, throttled via `push_throttled` so holding the mouse button doesn't spam it every frame)
- [x] Furnace (`tile_registry.FURNACE_ID`, `game/crafting/furnace_system.py`): a craftable station where ore + Coal (fuel) becomes a bar over real time, not an instant craft -- starting a smelt consumes the ore/fuel immediately but the bar isn't granted until `smelt_time_s` passes (shown as a progress bar in the crafting screen's "Smelting" section), and a furnace can only smelt one job at a time. Resolves 4 "catalog-only" gaps called out in earlier phases: Iron Ore, Topaz, Sapphire and Emerald all now smelt into their own Bar (`game/crafting/smelt_registry.py`)
- [x] Iron Bar has a genuine use, not another dead end: Iron Pickaxe (mining_power 5.0, between Stone's 3.0 and where Phase 9's tiered materials would continue) -- Topaz/Sapphire/Emerald Bars later combine into the Phase 9 Arcane Bar
- [x] Headless tests (`tests/test_crafting_and_furnace.py`, 26 tests): discovery locking/unlocking (including the "in inventory but never discovered" edge case), `collect_and_discover`/`craft_and_discover` announcing exactly the newly-unlocked recipes, `blocked_mining_reason` for the missing-tool/right-tool/out-of-reach/no-tool-needed cases, furnace start/busy/complete/restart lifecycle including insufficient ore or fuel, and the notification queue's ordering/expiry/throttling. Plus an updated `test_phase3_crafting.py` section-layout test and a full headless GameApp smoke test (mining-blocked message -> open crafting screen -> place a furnace -> start and complete a smelt -> item granted and discovered) exercising the real Renderer draw calls, not just the underlying logic

- [x] Crafting screen redesigned a second time (user feedback: the row-based layout "looked weird" -- inconsistent full-color-fill "ready" rows next to nearly-empty locked rows, a station label floating disconnected near the icon): rebuilt as an icon grid (several recipes side by side per row, `_CATEGORY_SECTION_ORDER` sections unchanged) with a fixed details panel on the right showing whichever cell is hovered (name, station badge, full ingredient breakdown, live status line) -- shared card language (`_draw_row_card`-style accent borders) replaced with a per-cell colored border only, no more full-color fills. Undiscovered recipes no longer render at all (not even as a locked placeholder) -- `_recipe_sections`/`_crafting_layout`/`crafting_max_scroll`/`recipe_at_screen_pos` all now take `discovered_item_ids` and filter before layout, so a category with nothing discovered doesn't even show its header
- [x] Headless tests updated for the grid (`tests/test_phase3_crafting.py`): undiscovered recipes are excluded from every section, a partially-discovered recipe never appears in `_crafting_layout` or hit-tests via `recipe_at_screen_pos`, plus the existing section/scroll tests updated to the new signatures

Known gaps, intentionally deferred:
- A furnace can only run one smelt job at a time (no queue) -- placing more furnaces is the way to parallelize -- **RESOLVED** by the "Crafting/furnace screen split + furnace queue" pass below (input hopper + auto-restart)
- No fuel-efficiency mechanic (e.g. different fuels burning longer) -- Coal is the only fuel, one fixed quantity per smelt
- Topaz/Sapphire/Emerald Bars now craft into an Arcane Bar (Phase 9)
- The "Materials" crafting-screen section includes the Arcane Bar recipe; other MATERIAL results still come from smelting
- With locked recipes hidden entirely, there's no in-grid hint that a whole category (e.g. Weapons) has more waiting to be discovered -- only what's already unlocked is visible, by design (see "How recipe discovery works" in README), but it does mean a player has no in-UI nudge toward what to go find next

## World-generated traps pass (user-requested: put the traps on the map): DONE
- [x] Redesigned Spikes/Fire/Arrow Trap/Falling Platform/Fan/Sand/Mud/Ice from player-craftable into world-generated environmental hazards (`TileDef.can_place=False`) -- found while exploring, not built, consistent with `assets/Traps/` being trap art rather than building-block art; Trampoline stays craftable (a bounce pad, not a hazard)
- [x] `world_generator._place_cave_hazard`: one roll per column (`CAVE_HAZARD_SPAWN_CHANCE_PER_COLUMN`) places at most one of Spikes/Fire/Arrow Trap/Falling Platform at a valid cave-floor spot (air tile, solid floor beneath, clear air above)
- [x] `world_generator._place_fan_shaft`: a rarer roll (`FAN_SHAFT_SPAWN_CHANCE_PER_COLUMN`) needing a taller find -- a floor with 4 clear tiles of air stacked above it, so the updraft has room to push the player somewhere
- [x] `world_generator._surface_tile_for`: each biome's surface row occasionally swaps for that biome's own surface trap (Desert -> Loose Sand, Snow -> Slick Ice, Jungle -> Sticky Mud, `SURFACE_TRAP_SPAWN_CHANCE_PER_COLUMN`) -- Forest has no entry in `_BIOME_SURFACE_TRAP_TILE`, so the always-Forest spawn window stays hazard-free
- [x] Fixed a crash left over from an unfinished prior session: `generate_column` called an undefined `_surface_tile_for`, so **every** column generation raised `NameError` -- 76 of 200 tests were failing before this pass
- [x] Updated stale tests from the earlier craftable-traps design (recipe/item registration assertions for spikes/falling_platform/fan/trap_mud/trap_ice) and widened the per-biome underground-tile-set assertions to allow the newly-legitimate cave hazard/fan tile ids
- [x] Headless tests (`tests/test_world_generator_second_pass_traps.py`): regression test that `generate_column` doesn't crash, cave hazards/fan shafts spawn in a wide scan with correct floor/clearance geometry, biome surface traps spawn in Desert/Snow/Jungle and never in Forest
- [x] Fixed a second unfinished-refactor bug found while testing this pass: `hazard_feature.SPIKED_BALL` (a 4th moving-hazard kind, already spawned in world gen and dealing contact damage) had no entry in `assets.load_hazard_textures`, so `Renderer._draw_hazard_sprite` crashed with `KeyError('spiked_ball')` the first time one was ever drawn -- wired up its real source art (`assets/Traps/Spiked Ball/Spiked Ball.png`) and added a regression test asserting every `hazard_feature.ALL_KINDS` entry has a texture

## Underground darkness (user-requested: caves/underground should actually be dark): DONE
- [x] `lighting.ambient_light_for_depth(depth_below_surface, sky_ambient)`: the day/night sky ambient now fades out below the surface, floored at `UNDERGROUND_MIN_AMBIENT` over `UNDERGROUND_DARK_DEPTH_TILES`, independent of time of day -- previously a cave at noon was exactly as bright as the open sky, so a Torch's light contribution was invisible until night
- [x] `World.surface_height_at(x)` (small refactor: `surface_spawn_y` now calls it instead of duplicating the `surface_height` import/call)
- [x] `Renderer._draw_world/_draw_player/_draw_enemies` each compute their own local ambient from depth before calling `lighting.light_level_at` (tiles per-column, entities per-position) -- everything below the surface fades toward near-black regardless of hour, and Torches/Fire matter down there exactly like they already did on the surface at night
- [x] Headless tests (`tests/test_phase5_world_cycle.py`): ambient unchanged at/above the surface, fades out underground even at noon, floored (never pitch black) even at midnight, `World.surface_height_at` matches the existing `surface_spawn_y`

Known gap, intentionally deferred: the darkness falloff is purely depth-based (distance below the fixed surface line), not true line-of-sight/occlusion -- a shallow open pit and an enclosed cave at the same depth get the same ambient, consistent with the lighting system's existing "no occlusion" simplification (see README "How the day/night cycle and lighting work").

## Phase 7 — Procedural structures + Save/Load: DONE
- [x] Chest (`tile_registry.CHEST_ID`): a lootable tile found only inside structures (not player-craftable/placeable, same as the world-generated traps), texture reused from `assets/Items/Boxes/Box2` (same pack as the wooden crate's Box1, previously unused). Reuses the existing `drop_pool` mechanism (Bush's "one of several possible drops") for its loot instead of a new loot-table system, drawing from already-registered valuable items (`iron_bar`, `topaz`, `sapphire`, `emerald`, `iron_pickaxe`, `wood_sword`, `wood_helmet`) -- no new items needed
- [x] `game/world/structures.py`: procedural structure placement, mirroring the tree-slot pattern (`world_generator._tree_center_for_slot`/`_tree_role_at`) at a much coarser slot width (`STRUCTURE_SLOT_WIDTH_TILES`) since structures are rarer/bigger -- a pure function of `(seed, slot_index)`, independent of which column asks, same determinism guarantee as everything else in world generation
- [x] Three structure kinds, each a hand-built 7-wide blueprint: **House** (surface, Wood Plank floor/walls/roof, doorway, Torch, Chest, Workbench -- worth walking into), **Ruins** (surface, Stone, no roof, each wall tile independently has `RUINS_WALL_COLLAPSE_CHANCE` of being left out so nature/terrain shows through the gaps, still guarantees a walkable interior + Chest), **Underground Room** (forcibly carved out of rock regardless of what was there -- walls/ceiling built from that column's own biome `underground_tile_id` so a Desert room reads as Desert Stone, not generic Stone; Wood Plank floor, Torch, Chest)
- [x] "Respecting terrain": a House/Ruins placement is skipped (rerolls to nothing) if the surface height varies more than `STRUCTURE_MAX_TERRAIN_VARIANCE_TILES` across its footprint, so nothing visibly floats over a cliff or gap
- [x] `structures.place_structures` runs last in `generate_column` (after cave hazards/fan shafts), so a structure's carve/overwrite always wins -- same last-applied-wins layering already used for decoration -> hazards -> fan shafts. `STRUCTURE_MARGIN_TILES` guarantees neighboring slots' footprints can never overlap
- [x] Headless tests (`tests/test_phase7_structures.py`): every kind spawns in a wide deterministic scan, no two structures overlap, every blueprint offset actually lands correctly through `generate_column` (real integration, not just the blueprint dict), House/Ruins floor sits at that column's own `surface_height`, the Chest is always reachable (solid floor beneath, open air above), Underground Room interior is genuinely hollowed out, and Underground Room walls use the correct per-biome stone (regression test for an early version that hardcoded generic Stone and broke the Phase 6 biome-purity tests)
- [x] `game/core/save_system.py`: pure `serialize`/`deserialize` (seed, `world.elapsed_s`, world clock, full player state including inventory/equipment/discovered items, in-progress furnace jobs, and **only modified chunks** -- diffed tile-by-tile against a freshly recomputed procedural baseline via `generate_column`, not saved whole) plus thin `save_to_file`/`load_from_file`/`save_exists` file wrappers, single slot (`SAVE_FILE_PATH` = `saves/save.json`, gitignored)
- [x] Two new pause-menu buttons, Save and Load (`GameApp.save_game`/`load_game`, wired through `InputHandler._handle_pause_click` exactly like the existing Restart button) -- hand-drawn floppy-disk/down-arrow icons since no Save/Load art exists in the pack, same precedent as the tool/weapon icons
- [x] Headless tests (`tests/test_phase7_save_load.py`): round-trip of player state/furnace jobs through `serialize`/`deserialize`, only dirty chunks are ever saved, a modified tile round-trips exactly and the rest of its chunk still matches the procedural baseline, real file round-trip via a temp path, missing-file load returns `None`, and a full end-to-end test through the actual pause-menu click path (`InputHandler` -> `GameApp`), not just the underlying functions

Known gaps, intentionally deferred:
- Tower is not implemented -- the game has no ladder/climbing mechanic (only single + double jump), so a tall vertical structure's loot would be unreachable; revisit once/if a climbing mechanic exists
- Structure terrain-matching is a variance check across the footprint, not true terrain-carving/leveling -- an extreme edge case simply skips placement (rerolls to nothing) rather than adapting the shape
- Save is single-slot -- no multiple save files or a picker UI
- Enemies, projectiles, particles, and on-screen notifications are not persisted -- they're transient/regenerable, same as what already happens after Restart
- Structure placement doesn't actively avoid the biome-surface-trap or moving-hazard-anchor systems when choosing a spot -- low collision probability, not engineered around (same reasoning already applied to e.g. trees vs. cave hazards)

## Inventory tooltip + right-click quick actions (user-requested): DONE
- [x] Hovering any occupied bag or equipment slot (while the inventory screen is open) shows a floating tooltip next to the cursor (`Renderer._draw_item_tooltip`): name colored by rarity, category/rarity line, wrapped `ItemDef.description`, and a category-specific stat block (`_item_tooltip_stat_lines` -- mining power for tools, damage + melee/ranged for weapons, defense/slot for armor, heal amount for food, durability/value where set). Clamped so it never overflows the window even hovering a slot in the corner
- [x] Right-clicking a bag item (`InputHandler._handle_inventory_right_click`): armor equips immediately (same transaction the existing left-click path already uses); anything else swaps places with whatever's in the *currently selected* hotbar slot via a new `Inventory.swap_slots(a, b)` -- a fast way to load the hotbar without dragging. Equipment slots aren't a right-click target (unequip already has its own left-click action)
- [x] Headless tests: `Inventory.swap_slots` (exchange, into-an-empty-slot, self-swap no-op) in `tests/test_phase1_smoke.py`; right-click equip/hotbar-swap/empty-slot-no-op, `_hovered_inventory_item` for bag/equipment/nothing, `_item_tooltip_stat_lines` varying by category, and a real `_draw_item_tooltip` render (including a stay-on-screen check near a window corner) in `tests/test_equipment.py`

## Summoner class (first character class, user-requested): DONE
- [x] `game/entities/class_registry.py` (`ClassDef`, same `_register`/`get`/`all_classes()` pattern as `character_registry.py`): two classes, **Warrior** (`allowed_weapon_classes=("normal",)`, reproduces exactly today's combat since every existing weapon defaults to `ItemDef.weapon_class="normal"`) and **Summoner** (`allowed_weapon_classes=("summon",)`, starts with a Twig Rod)
- [x] A second start-of-run select screen (`GameApp.class_select_open`), shown right after the existing character-skin picker closes -- same boolean-gate/`InputHandler`-event/`Renderer`-screen pattern as character select, but text cards (no per-class art exists) instead of portraits
- [x] `ItemDef` gained two additive fields (`weapon_class`, `summons_id`) so no existing item registration needed to change; `combat_system.try_attack` gates every attack on `item_def.weapon_class in class_def.allowed_weapon_classes` before dispatching to melee/ranged/summon, and surfaces a `Player.blocked_weapon_class_reason()` toast (mirrors `blocked_mining_reason`) when a class can't use the selected item
- [x] Two summon rods (`game/items/item_registry.py`), craftable like any other weapon (`recipe_registry.py`): **Twig Rod** (`wood x8`, anywhere -- also the Summoner's free starting item) summons a weak **Twig Sprite**; **Iron Rod** (`iron_bar x3, wood x5, slime_gel x2`, workbench) summons a much stronger **Iron Guardian** -- a second use for Iron Bar besides the Iron Pickaxe. Both land in the crafting screen's existing "Weapons" section with zero renderer changes
- [x] New `Summon`/`SummonDef`/`summon_ai.py` triple (`game/entities/`), mirroring the `Enemy`/`EnemyDef`/`enemy_ai.py` split: casting a rod (re)summons its minion at the player's position, replacing any currently active one -- only one summon is active at a time. `summon_ai.update` seeks the nearest alive enemy within `seek_radius_tiles` and flies at it, otherwise hovers near the player (snapping back if it strays past `follow_distance_tiles`); `combat_system.resolve_summon_attacks` deals the actual damage tick once in `attack_range_tiles`, keeping the same "AI moves, combat_system damages" split enemies already use
- [x] `player.class_id` round-trips through `save_system` (old saves without the field default to Warrior); `GameApp.restart()` carries `class_id` through and re-grants the class's starting item, same as it already carries `character_id`
- [x] Headless tests (`tests/test_summoner_class.py`, 17 tests): registry sanity, `blocked_weapon_class_reason` for every class/weapon combination, `try_attack`'s class gate both ways (including that a blocked melee swing lands no damage), casting-then-replacing a summon through the real `InputHandler` click path, `summon_ai` chasing/damaging a nearby enemy and hovering near the player with none around, the class-select screen's click+confirm path granting the starting item, and a full `GameApp` boot through both select screens ending with an active summon
- [x] Manually verified in the real (headless-SDL) renderer: the class-select screen, the Twig Rod's procedural icon in the inventory/hotbar, the crafting screen's Weapons section (Iron Rod correctly shows "not ready" without a nearby workbench, exactly like Stone Pickaxe/Wood Armor already did), and a live Twig Sprite flying to and damaging a nearby enemy

Known gaps, intentionally deferred:
- Only 2 classes (Warrior, Summoner) and 2 summon tiers -- proves the system end to end rather than building a full progression ladder in one pass
- Summons cannot take damage in this pass -- no enemy ever targets them (there's no "enemy attacks a friendly entity" combat path anywhere in the game yet); revisit if summons feel too safe
- Summons move flying-style (gravity ignored, no wall/ledge avoidance) -- the same deliberate simplification `enemy_ai._update_fly` already uses for Duskwing, needed here so a minion can follow the player anywhere (caves, gaps, mid-air) without ground-pathing AI
- No HUD indicator for the active summon (name, or a health bar if summons ever become damageable)
- A class restricts *attacking*, not *selecting* -- a disallowed weapon can still sit in the hotbar and be "held" (pre-aim facing still works), the attack itself is just a no-op with a toast

## RPG skill/leveling system (user-requested: character skills, leveling, magic level, defenses, XP): DONE
- [x] New `game/skills/` package (a subsystem alongside `game/combat/`, `game/crafting/`, `game/inventory/`): six independent RuneScape-style skills -- **Attack, Defense, Magic, Mining, Crafting, Hitpoints** -- each with its own XP bar and level, using the real RuneScape level-1-99 XP curve (`game/skills/xp_table.py`, `xp(99) = 13,034,431`, verified against the well-known reference values)
- [x] `game/skills/skills.py`'s `Skills` (one per `Player`, mirrors `Equipment`'s role): `add_xp` queues every level crossed onto `pending_level_ups` rather than pushing notifications itself, so XP-granting call sites (combat/mining/crafting) don't need to know about the UI -- `GameApp.step()` drains the queue once a frame and pushes a toast via the existing `NotificationQueue`, the same "New recipe unlocked" mechanism already used elsewhere
- [x] A 15-node skill-point tree (`game/skills/skill_tree_registry.py`, `SkillNodeDef`, same `_register`/`get` registry pattern as everything else): 3 tiers per skill (Attack/Defense/Magic/Mining/Crafting; Hitpoints has none) at levels 10/30/50, unlocked by spending points earned 1-per-level -- `Skills.available_points` is derived from `level - 1 - unlocked_count`, not a separately stored counter, so it can't desync from saved state. Tiers 2 and 3 also require the previous tier's node specifically (`SkillNodeDef.requires_node_id`), a real chain rather than just a level gate, so it reads as a tree to climb, not a flat list
- [x] Every skill has a real, wired-in effect: **Attack** boosts melee/ranged damage dealt (level curve + `attack_keen_edge`/`attack_berserker`/`attack_deadly_precision` nodes); **Defense** adds a flat bonus on top of equipment defense via a new shared `combat_system.player_total_defense(player)` helper (deduplicating 3 previously-copy-pasted `player.equipment.total_defense()` reads); **Magic** boosts the Summoner's summon damage, baked into the summon's per-instance stats at cast time (`Summon.damage`/`move_speed`/`attack_interval_s` now diverge from their `SummonDef`, the same way `Enemy.health` already diverges from `enemy_def.max_health`); **Mining** boosts mining power and (with `mining_deep_delver`) mine tick speed; **Crafting** boosts its own XP gain (`crafting_master`/`crafting_artisan`) and can refund an ingredient stack (`crafting_resourceful`, 15% chance); **Hitpoints** raises `player.max_health` on level-up (and heals by the same amount) and passively gains 1/3 of every Attack/Defense/Magic XP grant, the real RuneScape mechanic where every combat skill also trains survivability
- [x] XP hooks live inside the function that already owns the relevant transaction (no new call sites elsewhere): landed melee/ranged hits and kills in `combat_system._try_melee_attack`/`update_projectiles` (both gained a `player` parameter), summon hits in `resolve_summon_attacks` (also gained `player`), damage taken in `resolve_contact_damage`/`resolve_hazard_damage`/`resolve_hazard_feature_damage`, tile breaks in `Player.try_mine`, and successful crafts/smelts in `crafting_system.craft_and_discover`/`GameApp._update_furnace`
- [x] New "Skills" screen (`K`, joins the E/C/K mutual-exclusion group): a 6-row skill list with level + XP progress bar (left, same bar-fill technique as the health bar) and the selected skill's 3-node tree (right) -- nodes stacked vertically and connected by a line (gold once unlocked through, grey otherwise) so it visually reads as a tree, locked/unlockable/unlocked border colors, and a "Requires X first" reason shown when the level is high enough but the prior tier isn't unlocked yet
- [x] Character sheet (inventory screen, `E`) expanded into a real "at a glance" summary: the existing HP/Defense/Attack/Combat Lv block now sits above a compact 2-column, 6-entry skill-level grid (`_draw_skills_summary`) -- full per-skill XP bars and the tree itself still live in the Skills screen, this is just the summary
- [x] A new always-on top-left HUD bar (`_draw_combat_level_bar`, user-requested "barra no topo com o nível/%"): `Combat Lv N` plus a % progress bar (`Skills.combat_level_progress_ratio`, the average of Attack/Defense/Magic/Hitpoints' individual progress-to-next-level, since Combat Lv itself has no XP track of its own) -- wrapped in its own translucent panel, same technique as the existing top-center notification banner, so the white text stays readable regardless of sky color or bar fill
- [x] XP gain rates increased ~1.5x across the board (user feedback: leveling felt too slow) -- both the flat per-action XP amounts and the per-level passive stat percentages in `game/settings.py`
- [x] `player.skills` (xp + unlocked nodes per skill) round-trips through `save_system` (old saves without the field default to 0 xp / no nodes)
- [x] Headless tests (`tests/test_skills.py`, 36 tests, plus updates to `tests/test_phase4_combat.py`/`tests/test_summoner_class.py`/`tests/test_food_and_hazards.py`/`tests/test_phase7_save_load.py` for the new `player_total_defense` formula and the two combat_system signature changes): XP table reference values and monotonicity, `add_xp`/`drain_level_ups` single- and multi-level-up queuing, skill-tree registry sanity (including the tier-chain shape), `try_unlock_node` gating (level, points, already-unlocked, and the new prerequisite-tier check), every multiplier/bonus helper at baseline and with nodes, `level_progress_ratio`/`combat_level_progress_ratio`, every XP hook point (melee/ranged/summon/defense/mining/crafting/smelt), the Magic-boosted summon-damage-at-cast-time behavior, the `crafting_resourceful` refund roll, and a full `GameApp` test driving real level-up notifications and the Skills screen's click-to-select/click-to-unlock flow through the real `InputHandler`
- [x] Manually verified in the real (headless-SDL) renderer: the always-on top-left Combat Lv bar during normal gameplay, the Skills screen's connected 3-node vertical tree (unlocked chain in gold, an unlockable tier-3 node in green), and the character sheet's new skills summary grid -- no layout overlap anywhere

Known gaps, intentionally deferred:
- No Ranged/Strength split (Attack alone covers all weapon damage) -- there's no hit-chance/accuracy system in this game to hang a RuneScape-authentic Attack-vs-Strength split on
- XP amounts are flat per action (mining/crafting don't scale by tile tier or recipe complexity yet) -- easy constants to retune later, not a structural limitation
- No skill-tree respec -- points, once spent, stay spent
- No HUD element for individual XP-gain popups ("+12 Attack XP") -- the top bar shows aggregate Combat Lv progress and the level-up toast fires on an actual level-up, but there's no per-hit XP number floating up yet

## Phase 8 — NPCs: DONE
- [x] Three NPCs with distinct roles: **Guide** (dialogue tutorial, no shop, always present), **Merchant** (general goods + buy-back), **Blacksmith** (tools/armor)
- [x] Dialogue panel (T to talk while in range): sequential lines, Next/Shop/Close; walking out of range closes it
- [x] Shop inventory: coin currency earned by selling (`ItemDef.value` per item); listed prices strictly above value so buy-then-sell can't print money; infinite stock; inventory-full buy refunds the coins
- [x] Spawn conditions: Guide always; Merchant once bag wealth >= `NPC_MERCHANT_MIN_WEALTH`; Blacksmith once `iron_bar` is discovered. Homes prefer world-generated Houses, with a surface fallback near spawn so the Guide is findable
- [x] Arrival toast for gated NPCs; they stay once present even if the condition later fails; regenerated from conditions on load (not persisted, same as enemies)
- [x] Headless tests (`tests/test_phase8_npcs.py`): registry/price sanity, every spawn condition both ways, house-or-fallback standing positions, dialogue wrap, buy/sell/refund/can't-sell-coins, T-to-talk and shop clicks through InputHandler, full GameApp spawn + save/load without re-announcing

Known gaps, intentionally deferred:
- NPCs stand still at their home -- no walking, day/night schedule, or physics (mining the floor out from under one leaves them floating)
- No dedicated NPC art (flat colored humanoids + a nameplate, same gap as enemies)
- Shops have infinite stock and no unique-to-NPC items beyond convenience/gear-access; Coin has no sink besides shops
- The player cannot build housing for NPCs -- they use world-generated Houses (or a surface fallback)
- Guide dialogue is static, not branching on player progress

## Personal Chest (user-requested): DONE
- [x] Craftable Personal Chest (`tile_registry.PERSONAL_CHEST_ID`, 8 Wood, anywhere): place it, press **T** nearby to open a bag/stash transfer UI. Every chest shares `Player.personal_chest` (piggy-bank / ender-chest style) so breaking the tile doesn't lose stored items. Distinct from the world-loot Chest inside structures. Stash round-trips through save/load. Headless tests in `tests/test_personal_chest.py`

## Background music (user-requested): DONE
- [x] `game/core/music.py`: loads `Music/background.mp3` via `pygame.mixer.music` and loops it (`MUSIC_VOLUME` in settings.py), started once in `GameApp.__init__` right after `pygame.init()` so it's already playing on the title screen. Missing/corrupt file logs a warning and the game continues in silence rather than crashing, same precedent as missing art in `game/rendering/assets.py`

## Structure loot Chests (user-requested: random items in the abandoned structures, opened like the Personal Chest): DONE
- [x] `game/world/loot_chest.py`: the world-generated Chest tile inside Houses/Ruins/Underground Rooms (`tile_registry.CHEST_ID`) can now be opened with **T**, same panel/interaction as the Personal Chest, instead of only being mineable for one item. Each Chest's contents are 2-4 distinct items (`LOOT_CHEST_MIN_ROLLS`/`MAX_ROLLS`) drawn without replacement from a weighted pool (`LOOT_POOL`: bars/gems/gear rarer, coin/wood/torch more common) -- a pure function of `(seed, tile position)`, same determinism guarantee trees/ore/structures already rely on, so a given chest always re-derives identical starting loot the first time it's opened
- [x] `World.chest_loot`/`get_chest_inventory`: caches the rolled `Inventory` per tile position after first open (not eagerly for every chest in the world) so items taken out stay taken; `GameApp.open_chest_pos`/`active_chest_inventory` route the shared chest panel to either the player's own Personal Chest stash (`None`) or a specific world Chest's loot (a tile position) -- `InputHandler._handle_talk` still prefers a nearby Personal Chest first, falling back to a world Chest, matching the existing NPC-over-chest priority
- [x] Breaking the tile (mining) still works exactly as before -- one random item straight to the inventory via the pre-existing `drop_pool` -- as a destroy-it-and-go shortcut alongside the new open-and-loot-properly interaction
- [x] Only *opened* chests are saved (`save_system`'s `chest_loot` list, keyed by position); an untouched chest isn't written ahead of time since it re-derives the same roll on demand -- old saves without the field load with an empty chest cache
- [x] Headless tests (`tests/test_loot_chest.py`, 18 tests): roll determinism/position-variance/pool-membership/no-duplicate-items, `World.get_chest_inventory` caching and independence across positions, T-to-open (including Personal-Chest-first priority and toggle-closed clearing `open_chest_pos`), click-to-transfer both directions, closing when the player walks out of range, `active_chest_inventory` resolving personal vs. loot, save/load round-trip (touched vs. untouched vs. an old save missing the field), and a real `_draw_chest_panel` render for both storage kinds. Manually verified end-to-end against a real structure-generated chest (opened via `InputHandler`, rolled 4 distinct items)

## Full Map screen (user-requested: a map, opened with M, of the explored world): DONE
- [x] `game/world/exploration.py`: fog-of-war reveal -- the world is divided into `MAP_CELL_SIZE_TILES`-square cells; any cell within `MAP_REVEAL_RADIUS_TILES` of the player becomes "explored" and gets a representative color sampled once (its center tile's `TileDef.color`, or a fixed sky/cave-air color for open space depending on whether it's above or below `World.surface_height_at`) -- a deliberate one-sample-per-cell simplification, the same "no true detail, one value per region" tradeoff the lighting system already makes. `World.explored_cells`/`reveal_map_around` own the state; `GameApp.step` reveals around the player every frame it isn't paused (already-explored cells are a single dict-lookup skip, so the real sampling cost is only ever paid once per cell)
- [x] Map screen (**M**, joins the I/C/K/M mutual-exclusion group -- opening any one closes the others): `Renderer._draw_map_screen` builds a tiny pygame Surface (one pixel per explored cell, sized to just the bounding box of what's been explored so far) and nearest-neighbor scales it up to fill the panel, so the map visibly "grows" outward as the player wanders further rather than starting as a mostly-empty void. A red/white dot marks the player's live position; an empty map shows a "walk around" hint instead of a blank panel
- [x] `World.explored_cells` round-trips through save/load (`save_system`'s `explored_cells` list); an old save missing the field just loads with an empty (undiscovered) map
- [x] Headless tests (`tests/test_map.py`, 11 tests): reveal populates/bounds-checks nearby cells, is idempotent, reveals new cells after moving further away, cell coloring distinguishes sky/solid/cave-air, M opens/closes the screen and the I/C/K/M mutual-exclusion both ways, a real `GameApp.step()` loop actually reveals cells, save/load round-trip (including an old save missing the field), and a real `_draw_map_screen` render both empty and populated

Known gaps, intentionally deferred:
- No markers for NPCs/checkpoints on the map yet -- structures now are (see "Map structure markers" below)
- No click-to-set-waypoint or fast travel -- the map is read-only, matching the "no fast travel" scope of everything else in the game so far
- A cell's color is a single sampled tile, not an average/majority -- a mixed-terrain cell can look like whichever material happened to sit exactly at its center

## Map structure markers (user-requested follow-up: show discovered Houses/Ruins/Underground Rooms on the map): DONE
- [x] `renderer._discovered_structures(world, min_cx, max_cx)`: scans every structure slot (`structures.structure_for_slot`, already a cheap pure function of `(seed, slot_index)`) overlapping the map's currently-explored x-range, and keeps only the ones whose anchor cell is in `World.explored_cells` -- no new persisted state, "discovered" falls straight out of the existing fog-of-war data plus the existing deterministic structure registry, the same "just recompute it" precedent `save_system`'s chunk diffing already relies on
- [x] `Renderer._draw_map_structure_marker`: a small fixed-size shape per kind, distinct by silhouette (not just color) so it still reads in grayscale -- House a solid square, Ruins a hollow square (decayed/walls-missing, matching its own in-world look), Underground Room a solid diamond (a shape found nowhere on the surface). A legend row (icon + label for all three kinds) sits bottom-left of the map panel, the "M to close" hint bottom-right, both vertically centered on the same footer line so neither can overlap the other regardless of font metrics
- [x] Headless tests (`tests/test_map.py`, 4 more tests): a structure is discovered once its anchor cell is revealed, excluded when a different area is revealed instead, every marker kind draws without crashing, and a full `_draw_map_screen` render with a real discovered structure. Manually verified against a rendered screenshot with 6 real structures (2 Houses, 1 Ruins, 2 Underground Rooms, one undiscovered) -- correct shapes/colors, legend and hint don't overlap

Known gap, intentionally deferred: NPCs and Checkpoints still have no map marker of their own -- only the three structure kinds.

## Ground/flying AI movement fix (user-reported: several enemies -- and the summon -- couldn't handle uneven terrain): DONE
- [x] Root cause 1, ground enemies (Crawler/Scorpion, `AIType.WALK`): `enemy_ai._update_walk` treated *any* solid tile ahead as an unclimbable wall and reversed direction -- including an ordinary 1-tile rise in the terrain, not just a real wall. Ground AI has no jump input the way the player does, so on generated terrain's natural bumpiness this left ground enemies barely able to travel more than a tile or two before turning back. Fixed with `tile_collision.try_step_up`: if the blocking tile ahead is only 1 tile tall (the row directly above it is clear), the enemy snaps straight up onto it and keeps going instead of reversing; a taller, genuinely unclimbable wall (or a ledge with nothing to stand on across the gap -- the existing `has_ground_ahead` cliff check, unchanged) still turns it around
- [x] Root cause 2, flying entities (Duskwing/`AIType.FLY`, and the player's summon in `summon_ai.py`): both fly in a straight line toward their target via `tile_collision.move_axis`, which does enforce solid-tile collision -- so with no wall-routing logic, a wall/ledge directly between the flyer and its target just stopped it dead, pressed against the wall, every frame, instead of it flying up and over like it would any other obstacle. Fixed the same way in both `enemy_ai._update_fly` and `summon_ai.update`: when blocked, rise (negative y_vel) instead of holding still against the wall, so it climbs clear of the obstacle over the next few frames and then resumes its normal seek behavior
- [x] `tests/test_phase4_combat.py`'s existing `test_crawler_does_not_walk_off_a_ledge` was passing for the wrong reason -- the old always-reverse-on-any-bump behavior meant the crawler never got far enough from its spawn point to ever reach the pit being tested. Rewrote it on deterministic flattened terrain (a new `_flatten_ground` helper, bypassing world-gen noise) so it actually approaches and is stopped by the pit it digs, and added a companion test proving the reverse case: a real 1-tile ledge is now climbed, not avoided
- [x] Headless tests: `test_crawler_climbs_a_one_tile_ledge_instead_of_turning_around` and `test_crawler_does_not_walk_off_a_ledge` (both `tests/test_phase4_combat.py`) for ground AI; `test_duskwing_climbs_over_a_wall_instead_of_getting_stuck_against_it` (same file) and `test_summon_climbs_over_a_wall_instead_of_getting_stuck_against_it` (`tests/test_summoner_class.py`) for the two flying cases -- each builds a real wall/ledge with direct chunk tile-setting and asserts the entity makes real progress past it rather than stalling

## Four-agent audit + fixes (user-requested: parallel logic/visual/gameplay review, plus a new roadmap): DONE
Ran four parallel review passes over the whole codebase -- core logic/physics, rendering, a real playthrough, and a fresh roadmap draft (the last one lives in a separate artifact, not this file). Fixed everything actionable the first three turned up:
- [x] **Critical, this session's own regression:** `tile_collision.try_step_up` only checked headroom in the *ahead* column before snapping the entity up a tile -- not the entity's own current column. In a natural 1-tile-tall cave corridor (solid floor, solid ceiling directly overhead) with a climbable step ahead, that snapped the enemy up into its own ceiling tile, and the same bug re-triggered every following frame, tunneling it straight up through solid rock. Fixed by also requiring clearance directly above the entity's *own* current position, not just the column it's stepping into. Regression test: `test_crawler_does_not_tunnel_through_a_low_cave_ceiling`
- [x] **Critical, found by playtesting the climb fix itself:** a 1-tile-wide perch with an unclimbable wall on one side and an ordinary 1-tile step down on the other could trap a chasing ground enemy in a zero-net-progress oscillation -- direction is recomputed fresh from the player's position every frame, and the small y-offset from falling/settling was enough to nudge the enemy just outside `ENEMY_CHASE_RADIUS_TILES` every other frame, falling back to a stale `facing_right` that (before this fix) read the safe step-down as a cliff too, flipping it right back toward the wall. `x_vel` alternated +/-speed forever, never literally 0 (so a naive `x_vel == 0` check wouldn't have caught it) -- visibly frozen just out of melee reach. Fixed by giving `has_ground_ahead` the same 1-tile tolerance `try_step_up` already has for climbing: a small step *down* is ordinary bumpy terrain, not a cliff. Regression test: `test_crawler_does_not_freeze_on_a_narrow_perch_with_a_safe_step_down`
- [x] **Critical, pre-existing (not from this session):** `World._unload_far_chunks` deleted any chunk more than `CHUNK_UNLOAD_RADIUS` chunks from the player with no record of what had changed in it -- so mining/building near spawn, exploring far enough away, then saving lost those edits permanently (not just from live memory; `save_system.serialize` only ever looked at `world.chunks`, which no longer had the chunk in it at all). The code comment blamed this on "Phase 1 has no SaveSystem yet" -- stale; Phase 7 shipped save/load and this was never actually revisited. Fixed with `World._unloaded_chunk_diffs`: a chunk's diff against the procedural baseline is computed and cached right before it's unloaded (reusing a new `World.chunk_diff`, deduplicated out of `save_system`'s equivalent), re-applied if the player wanders back without saving, and included in `serialize()` alongside currently-loaded dirty chunks either way. Regression tests: `test_edits_survive_the_chunk_unloading_before_a_save`, `test_edits_survive_walking_away_and_back_without_saving` (`tests/test_phase7_save_load.py`)
- [x] Dead code: `Equipment.total_light_emit` had its entire body duplicated verbatim after its own `return` (unreachable, no behavior bug, clear copy-paste artifact) -- removed
- [x] Chest panel (Personal Chest and world loot Chest): the "Click a stack to move it · T to close" hint sat inside the 16px 9-sliced frame border's own thickness, so the border's trim line visually bisected the text. Moved it clear of the border
- [x] Title screen: the Load/Continue button's baked-in art says "LOAD", but the hint line under it said "Click **Continue** or press Enter to load" -- a naming mismatch between the button and its own caption. Changed the hint to say "Click **Load** or press Enter to continue"
- [x] Reviewed and left as-is (pre-existing, low-severity, or not clearly worth the churn): the crafting screen's near-empty-panel hint-text overflow (fixed properly in the very next pass below, once the user asked for a crafting pass anyway), a ~1px sliver of world visible under the populated crafting screen's bottom section, and the pause menu's Restart button label rendering slightly less saturated than its baked-art siblings

Known gaps surfaced but not fixed (left for a future pass, not urgent): a House's Chest loot alone can plausibly clear the Merchant's wealth threshold within a player's first couple of minutes. (The "mining feels frictionless" note from this pass was addressed directly below.)

## Mining & Crafting friction pass (user-requested: real weight to mining and crafting, not instant): DONE
- [x] Mining friction: `PLAYER_MINE_TICK_S` (how often one "hit" of `mining_power` lands while the mine button is held) raised 0.05s -> 0.2s -- ticks-to-break only depends on `ceil(resistance / mining_power)`, so this is the actual pacing lever. At the old value even a bare hand cleared dirt in one imperceptible tick and the wood->stone->iron->steel->arcane pickaxe ladder made almost no felt difference mining everyday blocks (confirmed by this session's own gameplay-agent audit above). Common blocks are still fast (1-2 ticks); tool-gated ore/gems now visibly reward a better pickaxe (up to several ticks with the starter one, one tick with a late-game one)
- [x] A mining outline (`Renderer._draw_mining_outline`, `Player.mining_target`/`mining_progress_ratio`): a gold border on the tile currently being mined, with a dark overlay that erodes from the top down as `_mining_progress` climbs toward the tile's `resistance` -- mining previously gave zero feedback beyond the block vanishing all at once
- [x] Crafting friction: crafting is no longer instant. `crafting_system.start_craft`/`update_pending_craft` (`CraftJob`, `Player.craft_job`) mirror `FurnaceManager`'s existing timed-job pattern -- ingredients are consumed the moment you click, same as starting a smelt, but the result isn't granted until `craft_time_for(recipe)` seconds pass. Only one craft runs at a time (a player has two hands, not N furnaces); clicking a different recipe while busy is a no-op with an "Already crafting something else" toast, same recipe again is a silent no-op. The original instant `craft()`/`craft_and_discover()` are untouched and still used by every existing test plus anything that wants a synchronous transaction -- `_apply_craft_rewards` was factored out so both paths grant identical XP/discovery/resourceful-refund rewards
- [x] `craft_time_for(recipe)` derives the duration from the result item's *existing* rarity tier (Common 0.4s -> Uncommon 0.8s -> Rare 1.4s -> Epic 2.2s) instead of a new per-recipe field -- every one of the 36 registered recipes gets a sensible, automatically-tiered time for free, already consistent with the same rarity border color the inventory/crafting screens draw around that same item
- [x] Crafting-screen feedback: the grid cell mirrors the Furnace's own in-progress look (a progress-filling bottom bar, gold border) for whichever recipe is mid-craft, and the details panel shows "Crafting: Xs left" / "Already crafting something else" / "Takes Xs" (now shown for every recipe, not just smelting) depending on status. A 10-particle Confetti burst (reusing the existing Checkpoint-activation effect, see `particles.py`) plays at the player on completion, alongside the usual "New recipe unlocked"-style toast
- [x] Fixed a real, previously-undocumented layout bug found while touching this screen: with **zero** recipes discovered (a brand-new run, before mining anything -- a state every fresh save starts in) the panel's content-fit height shrank to barely more than its own title bar, too short for the details panel's "Hover a recipe / for details" hint to fit -- it visibly spilled out past the panel's bottom border into the game world behind it. Gave `crafting_panel_height` a floor (`CRAFTING_MIN_PANEL_HEIGHT`) that only ever engages when there's little enough content to need it -- a panel with real content still sizes to fit it (and still scrolls past `CRAFTING_VIEWPORT_HEIGHT`) exactly as before
- [x] `Player.craft_job` round-trips through save/load, same treatment as furnace jobs (an old save missing the field just loads with no pending craft)
- [x] Headless tests (16 new, spread across `tests/test_crafting_and_furnace.py`, `tests/test_phase3_crafting.py`, `tests/test_phase7_save_load.py`, `tests/test_phase1_smoke.py`): craft-time-by-rarity ordering, `start_craft` consuming ingredients/creating a job/failing correctly (no ingredients, no station, already busy), `update_pending_craft` no-op-until-done and granting result+XP+discovery+resourceful-refund on completion, the real `InputHandler` click path starting a timed job instead of an instant grant (and not cancelling an in-progress job when a different recipe is clicked), a full `GameApp.step` loop confirming the notification/confetti/no-double-grant wiring, the crafting-panel min-height floor (both that it engages at zero content and that it doesn't cap a fully-populated panel), mining-outline `Player` accessors + a real `_draw_mining_outline` render (idle and mid-progress), and `craft_job` save/load round-trip (including an old save missing the field)

Known gap, intentionally deferred: per-recipe craft-time overrides don't exist -- every recipe of a given rarity takes exactly the same time, e.g. a 1-ingredient Torch and a 5-ingredient Steel Armor piece of the same tier would craft equally fast. Rarity-only was a deliberate simplification (zero new per-recipe data, see `craft_time_for`'s docstring) rather than hand-tuning 36 individual numbers; revisit if a specific recipe's pacing feels wrong in practice.

## Combat impact juice: hit-sparks + screen shake (user-requested, pulled straight from the roadmap's own "Quick wins" list): DONE
- [x] Hit Spark particles (`particles.py`'s new `HIT_SPARK` kind, `ParticleSystem.spawn_hit_spark`): a small radial burst of short fading streaks at every landed hit's position, no gravity (a straight outward burst, not falling debris). No source art exists for a spark, so it's drawn procedurally as fading lines in `Renderer._draw_hit_spark` -- the same "no art, draw it" precedent the melee swing arc already set, not a new sprite asset
- [x] Color reuses the existing hit-direction split instead of inventing a new one: `PLAYER_HIT_COLOR` (red) for a hit the player took, `ENEMY_HIT_COLOR` (gold) for one they dealt -- the same two constants the floating damage number already uses, so the spark and the number over a given hit always visually agree
- [x] Camera screen shake (`Camera.shake`/`Camera.update`, a new decaying `_shake_offset_x/y` folded into `world_to_screen` only): triggers when the *player* takes damage, scaled by the hit's own damage amount (clamped) so a Duskwing peck barely nudges the screen but the Slime King's 16-damage stomp reads as a real jolt -- no separate "this one's special" signal needed from `combat_system`, since every player-damage path (contact, hazard, boss stomp) already funnels through the same `take_damage`/`queue_popup` mechanism this hooks into. A shake already in progress is only replaced by a *stronger* one, never restarted/stacked, so a flurry of small hits doesn't read as a nonstop wobble. Dealing damage to an enemy does **not** shake the camera -- reserved for "you got hurt" moments, otherwise rapid melee combos against weak enemies would shake constantly
- [x] Deliberately kept `screen_to_world` shake-independent -- shake is a pure rendering offset, so mining/aim targeting during a shake never lands somewhere different from where the player actually clicked
- [x] Both hooked into the exact same place: `GameApp._harvest_damage_popups`, which already drains every landed hit for the floating-number system -- sparks/shake are just two more things that happen at that existing drain point, no new event-signaling plumbing
- [x] Headless tests (8 new, spread across `tests/test_deferred_assets_pass2.py`, `tests/test_phase1_smoke.py`, `tests/test_phase4_combat.py`): hit-spark burst count/color/radial-directions and no-gravity, camera shake offsetting `world_to_screen` and decaying to exactly zero, shake leaving `screen_to_world` untouched, a stronger shake replacing a weaker in-progress one (and a weaker one NOT interrupting), a real `GameApp.step` loop confirming sparks spawn and the camera actually shakes on a real hit, confirming dealing damage alone does *not* shake the camera, and a real `_draw_hit_spark` render (including after the particle's already expired)

## Sound effects (user-requested, straight off the roadmap's Cross-cutting/Quick-wins list -- "is it possible without audio files?"): DONE
- [x] `game/core/sfx.py`: 10 short sound effects -- `mine_tick`, `mine_break`, `melee_swing`, `ranged_shoot`, `hit_player`, `hit_enemy`, `craft_complete`, `smelt_complete`, `level_up`, `chest_open` -- synthesized from sine/square/triangle tones and white-noise bursts entirely in pure-stdlib Python (`array` + `math` + `random`), no audio files and no numpy. Same "no art, draw it" precedent this session's own Hit Spark particles and the melee swing arc already set, applied to audio: a short PCM waveform built at startup instead of a missing .wav/.mp3
- [x] A from-scratch synthesis toolkit (`_tone`, `_sweep`, `_noise_burst`, `_envelope`, `_concat`, `_mix`): a tone or a frequency sweep (a "whoosh"/"shoot"), a noise burst (an impact/crunch), linear-fade attack/release envelopes so nothing clicks at its start/end, sequencing tones back-to-back for a short chime (`craft_complete`, `level_up`), and layering two waveforms at once for a fuller hit (`mine_break`, `hit_player`) with int16-range clamping so a mix can never overflow into audible distortion
- [x] `hit_player`/`hit_enemy` reuse the exact same red/gold split as the Hit Spark particles and the floating damage number (`PLAYER_HIT_COLOR`/`ENEMY_HIT_COLOR`) conceptually -- a hit you took and a hit you dealt always sound distinct, hooked into the exact same `GameApp._harvest_damage_popups` drain point the particles/shake pass already uses, so no new event plumbing
- [x] Found and fixed a real crash while building this (not a pre-existing bug -- introduced and caught within this same pass): pygame's `mixer.Sound` objects are bound to whichever SDL_mixer session was active when they were constructed; caching one and playing it after that session is torn down (`pygame.quit()` -- concretely, exactly what happens between tests elsewhere in this suite that each build their own `GameApp`) is a native use-after-free, an access violation that took the whole test process down, not a catchable Python exception. Fixed by caching only the plain synthesized *bytes* (`_sound_bytes`, process-lifetime, session-independent) and having `play()` construct a fresh, cheap `Sound` from them against whichever mixer session is actually live at that exact moment, every single call -- never caching a `Sound` object itself
- [x] Also had to duplicate every mono waveform out to the mixer's own channel count (`_duplicate_channels`/`_output_channels`) before handing it to `Sound(buffer=...)` -- a channel-count mismatch gets silently misread as a different sample count entirely, playing back at the wrong speed/pitch with channels swapped (caught by comparing `Sound.get_length()` against the intended duration before it ever reached a real playback test)
- [x] Hooked into 8 real gameplay moments: `Player.try_mine` (a tick per successful hit, a break sound the instant a tile actually breaks), `combat_system._try_melee_attack`/`_try_ranged_attack` (swing/shot), `GameApp._harvest_damage_popups` (hit taken/dealt, alongside the existing spark+shake), `GameApp._update_crafting`/`_update_furnace` (craft/smelt completion, alongside the existing confetti), `GameApp._announce_level_ups` (level-up), and `InputHandler._open_chest_ui` (opening a Personal or loot Chest)
- [x] Headless tests (24 new, `tests/test_sfx.py`): every waveform builder (correct sample count/range, envelope actually fades instead of clicking, a sweep completes more cycles than a flat tone, noise is non-tonal and seed-deterministic, concat/mix/channel-duplication math), the full sound bank covers every key with real audio, `init()`/`play()` lifecycle (idempotent, unknown key and no-mixer-session both silently no-op), a direct regression test reproducing the crash's exact quit/reinit sequence and asserting it no longer crashes, and one integration test per gameplay hook (mining tick-then-break, melee/ranged, hit-player/hit-enemy via a real `GameApp.step`, craft/smelt completion, level-up, chest-open) confirming the right call site requests the right key via a monkeypatched `sfx.play`

Known gaps, intentionally deferred: no generic UI-click sound (button hovers/clicks across the pause menu, inventory, crafting grid, NPC shop) -- wiring that touches many more call sites across several files for comparatively small "feel" payoff next to the 10 gameplay moments above, left for a future pass if it turns out to be missed. No per-effect volume slider or a master SFX/music mix at the time -- resolved by the "Settings screen overhaul" pass below.

## SFX hardening (user-reported: mining and chest interaction "bugado e não funciona" right after the sound-effects pass landed): DONE
- [x] Root cause: `sfx.play()` only caught `pygame.error` around `pygame.mixer.Sound(buffer=...).play()`. This session's own three newest gameplay hooks for sfx -- mining (`Player.try_mine`), chest interaction (`InputHandler._open_chest_ui`), and crafting (`GameApp._update_crafting`) -- are exactly the three systems reported broken, a strong signal that an audio-backend quirk on a real machine (as opposed to this environment's dummy audio driver, which never exercises real hardware) raised something other than `pygame.error`, propagating uncaught straight through `GameApp.step`'s main loop with nothing to stop it. Widened `sfx.play()` (and `music.start_background_music`, same class of risk) to catch any `Exception` -- audio is enhancement, not core functionality, and must never be able to take mining/interaction/crafting down with it
- [x] While hardening this, also closed a real (if narrower) correctness gap in the same function: sound bytes were always duplicated to *stereo*, assuming pygame's mixer would be -- true by default, not guaranteed on every real device/driver. `_output_channels()` now reads the live mixer's actual channel count and `_duplicate_channels()` matches it, so a non-stereo mixer session can no longer be the reason a buffer is malformed in the first place
- [x] Headless tests (3 new, `tests/test_sfx.py`): channel duplication for mono/stereo/surround counts, and `_output_channels()` reading a real mixer's reported count (falling back to stereo when there's no live session). Manually verified end-to-end against a real `pygame.mixer.pre_init(channels=1)` mono session (previously untested) -- correct duration, no crash

## Phase 9 — Progression
- [x] More equipment slots: **Legs** and **Boots** (Accessory already existed). Character-sheet column compacted to fit 5 slots
- [x] Armor ladder **Wood -> Iron -> Steel** for Head/Body/Legs/Boots (each slot's defense strictly increases). Wood Greaves/Boots craft from wood; Iron/Steel sets need their bars at a Workbench
- [x] Next material tier after iron: **Steel Bar** (re-smelt 2 Iron Bars + 2 Coal at a Furnace) crafts a **Steel Pickaxe** (mining_power 7, above Iron's 5) and the Steel armor set. Blacksmith sells Wood Greaves and the Steel Pickaxe as a coin shortcut
- [x] Magic-tier: **Arcane Bar** (Workbench: 1 Topaz + 1 Sapphire + 1 Emerald Bar). Crafts an **Arcane Pickaxe** (mining_power 9 + 25% extra drop), an **Arcane Staff** (no-ammo Magic bolt, straight flight, trains Magic), and an **Arcane Helm** (defense above steel, emits light while worn so caves don't need a torch trail)

## Phase 10 — Bosses: DONE
- [x] First original boss, the **Slime King** (`ai_type=AIType.BOSS`, a new `Boss(Enemy)` subclass with its own `boss_ai.py`): a persistent top-of-screen health bar with a phase label, shown from the moment it's summoned (not just once damaged, like the small per-enemy bar)
- [x] Three health-ratio phases, each strictly adding to the last: **Phase 1** (>66%) hops toward the player, contact damage only. **Phase 2** (<=66%) adds a gravity-arced **Slime Lob** ranged projectile (`EnemyProjectile`/`combat_system.update_enemy_projectiles`, the enemy-owned mirror of the player's `Projectile`). **Phase 3** (<=33%) enrages once -- summons 2 regular Slimes, hops/lobs faster, and every landing triggers a radius **shockwave** (`Boss.stomp_pending` / `combat_system.resolve_boss_stomp`) that can hit the player without a direct hitbox overlap
- [x] Summon condition: craft a **Slime Core Idol** (Workbench: 20 Slime Gel + 5 Iron Bar) and press **G** (`Player.use_selected_summon_item` -> `GameApp.try_summon_boss`) to spawn it beside the player; blocked while one is already active so idols can't be wasted
- [x] Exclusive drops: guaranteed **Slime King's Core** (`drop_chance=1.0`) plus a coin bounty (`BOSS_COIN_BOUNTY`). The Core isn't a dead end -- it crafts into the **Crown of the Slime King** (Workbench: 1 Core + 3 Steel Bar), the best head armor in the game (defense above the Arcane Helm)
- [x] Headless tests (`tests/test_phase10_boss.py`, 16 tests): registry/spawner safety (a boss is never picked by `EnemySpawner`'s weighted roll), idol consumption, summon blocking while one is active, phase transitions and their distinct attacks (ranged lob, one-shot enrage minions, landing stomp), enemy-projectile collision/defense/expiry, and the exclusive-drop + coin-bounty payout on defeat. Plus a full real-game-loop smoke test (summon -> fast-forward to phase 3 -> minions spawned, player takes real damage)

Known gaps, intentionally deferred:
- Only one boss exists -- no arena/gate mechanic keeps the player from wandering off mid-fight, and there's no cross-boss progression curve yet
- The boss isn't validated to spawn on solid ground (same "just spawn at the player's position" simplicity the Summoner's summon rod already uses) -- summoning right at a cliff edge or underground could drop/wedge it awkwardly
- The Slime King now reuses the regular Slime's real sprite art, scaled up (`SLIME_KING_SPRITE_SIZE`) and tinted redder per phase (`_boss_phase_tint`, multiplied onto the sprite) -- added in a later pass, not bespoke boss art, and still no audio cues (roar, phase-transition sting)
- Bosses aren't persisted across save/load, same transience as regular enemies/projectiles (see save_system.py) -- reloading mid-fight discards it (the idol is already consumed, though, so the player fully loses that attempt)

## Boss retuning (user-reported: "ele nem tá me acertando no pulo direito, crie projéteis, deixe ele mais forte"): DONE
- [x] **Root cause of the whiffed hops**: the Slime King's hop aimed at wherever the player happened to be standing *the instant it launched*, then committed to that fixed horizontal velocity for the entire arc -- an easy dodge, made worse by `SLIME_KING_MOVE_SPEED` (4.5) actually being *slower* than the player's own move speed (5.5), so a player who just walked away could outrun every hop indefinitely
- [x] The hop now leads the target (`boss_ai._update_hop_movement`): it solves for the player's *predicted* position at landing time (current position + their own `x_vel` * the arc's time-of-flight, itself derived from `SLIME_KING_HOP_IMPULSE`/`GRAVITY`) rather than their position at launch, capped at `move_speed * phase_mult * SLIME_KING_HOP_LEAD_SPEED_MULT` so a very distant target doesn't demand an absurd lunge. `SLIME_KING_MOVE_SPEED` is now 6.5 -- faster than the player outright -- and hops fire more often (`SLIME_KING_HOP_INTERVAL_MIN/MAX_S` down from 0.5-1.1s to 0.35-0.8s)
- [x] "Crie projéteis" (plural): the ranged attack is a single Slime Lob through phase 2, same as before, but once enraged (phase 3) it fans out into `SLIME_KING_SPREAD_COUNT` (3) projectiles at once (`boss_ai._fire_projectiles`, `SLIME_KING_SPREAD_ANGLE_DEGREES` apart) -- dodging one lob is easy, dodging a spread means finding the gap between them. Cooldown also dropped (2.2s -> 1.8s baseline)
- [x] "Deixe ele mais forte": health 480 -> 620, contact damage 24 -> 30, Slime Lob damage 14 -> 18, phase-3 stomp shockwave damage 16 -> 22, coin bounty on defeat 60 -> 90 (the reward scales with the fight getting harder)
- [x] `boss_ai.update`'s return type changed from an `Optional[EnemyProjectile]` to a `List[EnemyProjectile]` (zero, one, or `SLIME_KING_SPREAD_COUNT` at once) to support the phase-3 spread -- `GameApp.step` now `.extend()`s `enemy_projectiles` instead of appending a single optional one
- [x] Headless tests (`tests/test_phase10_boss.py`, grew from 16 to 24): phase-2 still fires exactly one lob, phase-3 fires exactly `SLIME_KING_SPREAD_COUNT` distinctly-aimed projectiles, the hop leads a fleeing player harder than a stationary one, the lead speed is capped not unbounded, and `SLIME_KING_MOVE_SPEED > PLAYER_MOVE_SPEED`. Also fixed a latent bug this surfaced in the test helper itself: `_make_boss` positioned the Slime King the same way regular ~1-tile enemies are positioned in other tests, but at 1.8 tiles tall that embeds its bottom into the solid ground, which then read as a horizontal wall collision (zeroing `x_vel`) the instant a test tried to hop it sideways -- real gameplay was never affected (`GameApp.try_summon_boss` spawns it at the player's height and lets it fall+land normally, which already resolves flush), but the test helper now lands it exactly on the ground's top the same way `test_ground_and_wall_probes` already does for regular enemies

## Boss retuning, round 2 (user-reported: "nao vi nenhum projetil... um bloco já impede ele de me bater"): DONE
- [x] **Why no projectiles were ever seen**: the Slime Lob only unlocked at phase 2 (health <=66%) -- a fight the player lost, fled, or simply didn't press hard enough never got the boss that low, so "crie projéteis" from the previous pass was implemented but effectively invisible in real play. The lob now fires from phase 1, i.e. from the very start of the fight (`boss_ai.update`, `ranged_cooldown_remaining` no longer gated behind `phase_index >= 1`); phase 2/3 only change its cadence, and the fan-out spread is still phase-3-only
- [x] **Why a single block stopped the boss cold**: `_update_hop_movement` only ever *launches* `x_vel` at the moment a hop starts and never revisits it mid-air, and `tile_collision.move_axis` zeroes `x_vel` the instant any part of the mover's hitbox clips a solid tile. At `SLIME_KING_HEIGHT_TILES` (1.8) the boss's hitbox spans two tile rows, so a 1-tile floor bump anywhere under its arc -- something a ~1-tile regular enemy's hop clears without ever registering a collision -- zeroed its horizontal velocity outright, stranding it at the bump until the next hop cycle re-aimed from scratch
- [x] Headless tests (`tests/test_phase10_boss.py`, still 20 -- `test_boss_phase1_has_no_ranged_attack` renamed/rewritten to `test_boss_phase1_already_fires_a_single_lob_and_no_minions` to match the new phase-1 behavior instead of dropping coverage)

## Ground AI step-up, generalized (user-reported: boss and regular mobs "fica pulando no mesmo lugar infinito" against a single block): DONE
- [x] The boss fix above was first written as its own boss-only helper, but the same "hop only sets x_vel at launch, never revisits it mid-air" shape is shared by every `AIType.HOP` regular enemy's `_update_hop` (`enemy_ai.py`) -- which never called any step-up logic at all (only `AIType.WALK` did, via `tile_collision.is_solid_ahead`/`try_step_up`). A regular Slime hitting the same kind of 1-tile bump showed the identical symptom: launch a hop toward the player, clip the bump, zero out, land, re-aim at the same spot next cycle, repeat forever -- reads as "pulando no mesmo lugar infinito"
- [x] Deleted the boss-only helper and generalized the shared one instead: `tile_collision.is_solid_ahead`/`try_step_up` used to probe only a single row at the entity's mid-height (fine for a ~1-tile enemy, blind for anything taller). Both now check every row the entity's height actually spans (`tile_collision._entity_row_span`) -- a no-op behavior change for every existing ~1-tile enemy (their span is always one row already), but makes the functions correct for the boss too, so `boss_ai._update_hop_movement` and `enemy_ai._update_hop` both just call `tile_collision.try_step_up` directly now, no bespoke boss copy
- [x] `_update_hop` (regular enemies) now calls `try_step_up` every frame while `x_vel != 0`, the same "climb a clean 1-tile bump with headroom above, leave a real wall alone" rule `try_step_up` already enforced for `WALK`-type ground AI
- [x] Headless tests: full suite green (546). Ad hoc simulation scripts (not committed) confirm a regular Slime now hops clean past a 1-tile bump the same way the boss does, and that a genuine 2+-tile wall still blocks both. Fixed one pre-existing borderline test this surfaced: `test_summon_climbs_over_a_wall_instead_of_getting_stuck_against_it` asserted the summon's *final-frame* altitude was above its start, but by frame 240 it had long since cleared the wall and settled into a small orbit around its (stationary) combat target near the wall's own height with no attack-range standoff of its own -- a coin flip on which side of the threshold that orbit lands on, worsened (not caused) by the more sensitive `is_solid_ahead`. Rewritten to assert the *minimum* altitude reached during the run instead, which is what the test actually means to check

## Boss still stuck on uneven terrain, round 3 (user-reported: "o boss ainda fica preso em um terreno desnivelado"): DONE
- [x] **Root cause**: `is_solid_ahead`/`try_step_up` probed the tile column at `center + direction`, one column past the entity's own center -- correct for a ~1-tile-wide regular enemy, but the Slime King is `SLIME_KING_WIDTH_TILES` (2.4) wide, so its *actual* leading edge (what `tile_collision.move_axis`'s own collision check resolves against) can already be standing a full tile or more past that probe column. Confirmed with a 60-real-second simulation on real generated terrain (`DEFAULT_SEED`, right next to the world's own spawn column): a perfectly smooth staircase where each column is only ever 1 tile taller than its neighbor still froze the boss permanently, because its wide leading edge caught a *2-tile* total rise across its own footprint that the single center-based probe column never even looked at, let alone tried to climb
- [x] Added `tile_collision._leading_tile_column(entity, direction)`: uses the entity's real edge column when it's wider than one tile (`entity.width > TILE_SIZE`), otherwise falls straight back to the exact old center-based column -- a deliberate no-op for every existing ~1-tile enemy, so this only changes anything for the boss. `is_solid_ahead`/`try_step_up`/`has_ground_ahead` all switched to it
- [x] `try_step_up` gained a `max_step_tiles` parameter (default 1, unchanged for every existing caller): scans upward from the entity's own bottom row at the probe column until it finds clear space or hits the cap, then snaps up by exactly that many tiles if there's headroom -- rather than the old fixed "exactly one tile or nothing." `boss_ai._update_hop_movement` now passes `max_step_tiles=2`, matched to the 2-tile worst-case rise its width can catch on this game's 1-tile-per-column terrain; a genuine 3+-tile wall is still capped out and correctly left blocking it
- [x] Headless tests (`tests/test_phase10_boss.py`, 20 -> 22): `test_boss_climbs_a_smooth_staircase_instead_of_getting_stuck` builds the same kind of terrain that froze it in the real-world simulation and asserts real progress across it; `test_boss_still_blocked_by_a_genuinely_tall_wall` locks in that the fix didn't quietly turn into "the boss can climb anything." Both seed the global RNG (`random.seed(0)`) since hop cadence (`random.uniform`) is otherwise shared, unseeded state across the whole pytest session -- the first version of the staircase test was borderline-flaky for exactly that reason before being pinned. Full suite green (548)

Known gaps, intentionally deferred:
- `max_step_tiles=2` is a width-matched constant for the *current* boss dimensions (`SLIME_KING_WIDTH_TILES`), not derived from `entity.width` automatically -- a future boss/enemy much wider than 2.4 tiles would need its own tuned value
- Still no real pathfinding: a wall taller than 2 tiles (or 1, for every non-boss enemy) simply stops the chase dead rather than routing around it -- fine for the "just climb natural bumpy terrain" case this was about, not a general solution for player-built structures

## Trees no longer block movement (user-reported: "a arvore nao pode impedir um mob ou um player de andar, ela tem que estar atrás"): DONE
- [x] `TREE_TRUNK_ID`/`TREE_LEAVES_ID` (`game/world/tile_registry.py`) flipped from `solid=True` to `solid=False`. World generation only ever places trunk/leaves tiles *above* the ground column they grow from (`world_generator._tree_role_at`), so this doesn't touch the walkable surface tile itself -- it just stops the tree's own 3-tile-tall body from acting as a wall for the player or any ground-walking enemy
- [x] No render/z-order change needed: `Renderer.draw` already paints `_draw_world` (all tiles, trees included) before `_draw_enemies`/`_draw_player`, so trees were already drawn behind entities -- the reported problem was purely collision (`TileDef.solid`), not draw order
- [x] Verified via a headless script: `world.is_solid(x, y)` is `False` at every generated tree-trunk/leaves tile; full suite (546 tests) still green with no test asserting tree solidity

## UI reskin (user-requested: review a supplied asset pack in assets/validar for usable sprites): DONE
- [x] Surveyed `assets/validar` (a top-down zombie-survival pack): its character/enemy/nature art is drawn top-down and is thematically a shooter, so none of it fits this side-view fantasy-mining game -- see README "How the UI theme works". Its `UI/` folder, plain 2D chrome with no perspective to clash, reskins every panel/button/slot in the game instead
- [x] Health bar: the flat rectangle became a proportional bar drawn from the pack's heart-tipped `HP-Bar.png` frame + `HP.png` fill strip (`Renderer._draw_hp_frame_bar`), shared by the player HUD bar and the Phase 10 boss bar so both read as the same UI language
- [x] Every panel (Inventory/Crafting/Skills/Personal Chest/NPC dialogue -- all share `_draw_panel_chrome`) now renders a 9-sliced wood/stone frame (`Renderer._draw_nine_slice`, generic and cached) instead of a flat rounded rect, at each panel's own size with no layout changes
- [x] Every item slot (bag, equipment, hotbar, crafting grid cell, chest, NPC shop bag -- `_draw_item_slot` plus the hotbar/recipe-cell call sites) now draws the pack's cell texture (a distinct "chosen" variant for the selected/highlighted slot) with the existing rarity/status-colored border kept on top, so ready/missing/needs-station color-coding still reads at a glance
- [x] Every button (title screen, pause menu + its Settings sub-screen, NPC dialogue Shop/Next/Talk/Close) now draws a wood-plank button (`_draw_ui_button`), swapping in the pack's own "pressed" art on hover; buttons whose baked-in text already matches the action (Play/Save/Load/Settings/Quit) skip drawing a redundant label, the rest use the textless "Blank" plank with a drawn label. Pause buttons changed from square icon buttons to landscape plaques (`PAUSE_BUTTON_WIDTH/HEIGHT`) to match the art's own proportions
- [x] Removed the now-fully-unused `assets/Menu/Buttons/Menu_jogo` icon loader (`load_menu_button_icons`/`_build_save_icon`/`_build_load_icon`) rather than leave dead code behind

Known gaps, intentionally deferred:
- A crafting panel sized for almost no discovered recipes (near-empty save) renders as a very short strip where the hovered-recipe hint text can visibly overflow its bottom edge -- a pre-existing layout quirk (the flat-rect version had the same tiny panel, just less visually obvious), not something this pass introduced or fixed
- The Skills screen's per-node tree cards and per-skill XP row list still use the old flat-rect look (only the outer panel/header was reskinned) -- lower priority since they're plain text lists, not slot grids
- No dedicated boss/enemy/player sprite art came out of this pass -- see each of those "Known gaps" sections above; the validar pack's character art doesn't fit (see the survey note above)

## Settings screen overhaul (user-requested: "melhore o settings do jogo, controle de audio etc"): DONE
- [x] The pause menu's Settings sub-screen is now a real panel (`Renderer._draw_settings_panel`, reusing the UI reskin's `_draw_panel_chrome`) instead of 3 bare buttons floating on the dark overlay: one row per adjustable value (Zoom, Music Volume, SFX Volume), each a `-`/`+` stepper around a live percentage, plus a Back button
- [x] `game/core/settings_store.py` (new): a small `Settings` object (`music_volume`, `sfx_volume`) persisted to `saves/settings.json`, separate from the save slot on purpose -- preferences aren't part of "one playthrough" and must survive Restart/Load untouched (unlike `world`/`player`/etc, which those rebuild from scratch). A missing or corrupt file degrades to defaults rather than blocking boot, same precedent `sfx.py`/`music.py` already set for audio
- [x] `music.py` gained `set_volume`/a `start_background_music(volume=...)` param, `sfx.py` gained `set_volume` (a master gain applied per-`Sound` at `play()` time, not baked into the cached waveform bytes) -- both wired to live `GameApp.prefs`, loaded once in `GameApp.__init__` (not `_new_run`) and applied immediately on every adjustment, no restart needed to hear the new level
- [x] A new tiny `ui_tick` sfx plays on every SFX Volume adjustment, doubling as instant feedback for "how loud is that now"
- [x] Headless tests (`tests/test_settings_screen.py`, 14 tests, plus one exact-set update in `tests/test_sfx.py`): settings_store load/save round-trip, defaults on a missing file, clamping out-of-range values, degrading to defaults on a corrupt file, `GameApp.adjust_music_volume`/`adjust_sfx_volume` clamping + live-applying + persisting, a full boot-time load-and-apply chain, Restart/Load leaving prefs untouched, settings-panel row/button layout (no overlap, stays inside the panel), and the real click path through `InputHandler` for every action (zoom, both volumes, Back)

Known gaps, intentionally deferred:
- Still no master mix/mute-all toggle -- Music and SFX are independent sliders only
- No volume control for the title screen (the pause menu's Settings only exists once a run is in progress) -- background music already plays there, just not adjustable until you start/continue a game
- Mouse buttons, hotbar 1-9, Esc and the F-keys are not rebindable (rebinding them would brick pause, the hotbar, or debug with no way back into Settings)

## Day/Night & Underground mob density (user-requested: "de dia quantidade de mobs reduzidos, a noite a quantidade aumenta, o subsolo é perigoso, cheio de mobs"): DONE
- [x] `EnemySpawner` now picks its population cap and spawn cadence from *where and when* the player is (`_current_limits`), instead of one fixed `ENEMY_MAX_ALIVE`/`ENEMY_SPAWN_INTERVAL_S` for the whole game: `ENEMY_MAX_ALIVE_DAY`(5)/`_NIGHT`(10) at the surface, `ENEMY_MAX_ALIVE_UNDERGROUND`(14) -- the most dangerous of all -- once the player is `UNDERGROUND_SPAWN_DEPTH_TILES`(4) below the surface, at *any* hour, since caves are already dark regardless of the surface day/night cycle (see "How lighting works"). Spawn cadence (`ENEMY_SPAWN_INTERVAL_DAY_S`/`_NIGHT_S`/`_UNDERGROUND_S`) scales the same way
- [x] The real gap this closes: enemies previously never spawned underground *at all* -- `EnemySpawner._try_spawn` always placed new enemies at the world surface, no matter how deep the player was. A new `_underground_spawn_y_tiles` scans vertically near the player's own depth in a random nearby column for a real cave spot (a solid floor beneath open air for ground mobs; just one open tile for flyers, which ignore gravity/collision) and skips the spawn attempt entirely if nothing suitable turns up nearby, rather than silently falling back to the surface
- [x] Existing enemies (Crawler, Slime, Scorpion, Duskwing) all now show up underground too, using the same biome-weighted roster (Scorpion still desert-only) -- no new enemy types needed, "the roster you'd meet on the surface, but there's more of it and it's always dark" was already the request
- [x] Headless tests (`tests/test_day_night_density.py`, 9 tests): `_current_limits` for day/night/underground (and that a shallow terrain dip doesn't falsely trigger "underground"), `_underground_spawn_y_tiles` finding a hand-carved cave floor / returning None in solid rock / a flyer needing no floor, and full spawn-loop tests proving the right cap holds for the day, night, and underground cases (the underground one carving a long tunnel so random spawn attempts reliably land, then asserting spawned enemies actually appear near the player's depth, not at the surface)

Known gaps, intentionally deferred:
- Underground danger is mob density only -- hazard/structure/ore spawn chances (Spikes, Chests, gem veins, etc.) are unchanged; those already exist from earlier phases and weren't re-tuned in this pass
- No depth-based enemy *stat* scaling (deeper = stronger) -- an underground Crawler hits exactly as hard as a surface one, just shows up more often

## Building: Doors, Beds & Sleeping (user-requested: "sistema de construção, portas, casas, cama... ao dormir a hora passa mais rápido"): DONE
- [x] **Door** (`tile_registry.DOOR_CLOSED_ID`/`DOOR_OPEN_ID`, craftable: 6 Wood at a Workbench): press **T** nearby to toggle between a solid (closed) and passable (open) tile -- the same "swap the actual stored tile id" trick Falling Platform already uses for its crumble/respawn transition (`World.set_tile`, a new generic public method), just player-driven instead of timer-driven (`game/world/doors.py`). Breaking either state drops the item back, so opening a door never costs you the ability to pick it back up. Houses themselves need no new tile type -- the existing placeable Wood Plank/Stone Blocks are the walls; Door is what keeps you from being sealed inside your own build
- [x] **Bed** (`tile_registry.BED_ID`, craftable: 8 Wood + 4 Wood Plank at a Workbench): press T nearby to sleep and skip straight to morning (`WorldClock.skip_to_morning`, always moving forward in time -- to tomorrow's dawn if already past today's, later today otherwise), but only **at night** and only inside a real **enclosed shelter**
- [x] Shelter detection (`game/world/shelter.py`): a bounded flood-fill (BFS) from the bed's own tile through every reachable non-solid tile. Exhausts within `SHELTER_MAX_AIR_TILES`(200, ~14x14 open interior) -> a real bounded room, sleep allowed. Still has unvisited tiles when the cap hits -> the space leaks into the open world (or is just too big to read as a cozy room) -> sleep refused with "This isn't a safe, enclosed shelter". No new "roof" tile needed -- *any* solid block (including ordinary building materials) works as a wall/ceiling, the algorithm only cares about solidity, not tile type
- [x] A brief black-screen fade-out plays right after a successful sleep (`GameApp.sleep_fade_remaining_s`/`SLEEP_FADE_DURATION_S`, `Renderer._draw_sleep_fade`, drawn over everything including the pause overlay) -- an instant clock jump otherwise reads as a glitch, not a night passing. Plus a new soft descending `sleep` sfx (the mirror image of `level_up`'s ascending chime) and a "Slept through the night" toast
- [x] Headless tests (`tests/test_building.py`, 20 tests): item/recipe registration, Door solid-state swap (and no-op on a non-door tile), `shelter.is_enclosed` for a sealed room / open sky / a room that leaks through a roof gap / a room too big to count / a solid starting point, `WorldClock.skip_to_morning` from both before and after dawn (and that it never moves time backward), `beds.try_sleep`'s three outcomes (day-blocked, unsheltered-blocked, success), and the real T-key click path through `InputHandler` for both Door and Bed (including the day-blocked case leaving the clock untouched) plus a full `GameApp.step()` render with both tiles placed and the sleep fade active

Known gaps, intentionally deferred:
- No dedicated Door/Bed art -- flat `TileDef.color` swatches, same "no art yet, falls back to a flat color" precedent most of this project's newer tiles already have (see `Renderer._tile_texture`)
- Doors don't auto-close, animate a swing, or block enemy pathing specially (enemies don't path around obstacles at all currently, open or closed makes no AI difference to them)
- No world-select/multiple-beds spawn-point integration -- a Bed only skips time, it doesn't also become a respawn point (that's still Checkpoint's job)
- Sleeping doesn't clear nearby enemies or otherwise "reset" danger -- time skips, whatever was already spawned nearby stays exactly where it was

## Debug tools (user-requested: "método debug, com tudo que tem direito -- receitas, teleport, mapa liberado, spawn de bosses e de todos os bixos"): DONE
- [x] **F4** toggles `GameApp.debug_mode`, a dev-session flag (like `prefs` -- not reset by `_new_run`/Restart/Load) gating seven cheats on **F5-F11**, so a stray keypress during normal play can never trigger one. A red "DEBUG MODE" banner + shortcut list shows on screen whenever it's on, independent of F3's own overlay
- [x] **F5** unlock all recipes (marks every registered item id discovered -- recipe visibility is ingredient-discovery-based, not a recipe-id allowlist, see `crafting_system.is_recipe_discovered`) -- **F6** reveal the whole map (`World.reveal_map_fully` -> `exploration.reveal_all`, the same per-cell sampling the real Map/M fog-of-war uses, just with no radius limit) -- **F7** full heal + 999 Coins -- **F8** spawn one of every registered boss (`AIType.BOSS`, currently the Slime King) as a real `Boss`, bypassing `try_summon_boss`'s idol/one-at-a-time gating -- **F9** spawn one of every non-boss enemy type -- **F10** teleport to the mouse cursor's world position -- **F11** teleport back to the last Checkpoint (or world spawn)
- [x] Headless tests (`tests/test_debug_tools.py`, 14 tests): every `debug_*` method in isolation, the real F4/F5-F11 click path through `InputHandler` (including that F5-F11 are true no-ops while debug_mode is off), `debug_mode` surviving Restart, and a full `GameApp.step()` render with the banner + every enemy/boss type spawned at once
- [x] Fixed a pre-existing (not introduced by this pass) visual bug while touching `debug_overlay.py`: the F3 overlay's text started at the exact same top-left corner as the always-on Combat Lv HUD panel, overlapping it -- both the F3 overlay and the new debug-mode banner now start below it (`_TOP_MARGIN`)

Known gaps, intentionally deferred:
- No give-item/regen-world commands -- "unlock all recipes" + "full heal + coins" cover the practical "I need resources to test crafting" case without a full item-picker UI; regen-world is a destructive action (discards the current world) that felt too risky to bind to a bare keypress without a confirmation step this pass didn't build
- Debug spawns/teleports don't validate the destination is safe (solid ground, not inside rock) -- same "developer convenience, not a polished feature" scope as the rest of this list

## Real item icons from a user-supplied sheet (user-provided: "coloquei um png na pasta items" -> "use tudo oque conseguir desses sprites" -> "tudo mesmo"): DONE
- [x] The user dropped `assets/Items/#2 - Transparent Icons & Drop Shadow.png` (~350 hand-drawn fantasy RPG icons, 16 columns x 32px cells) into the items folder. First pass wired up 11 single-icon, no-tier-ambiguity matches: **Wood Sword**, **Wood Bow**, **Grapple Hook**, **Slime Gel**, **Feather**, **Coin**, and the five ore items (**Coal/Iron Ore/Topaz/Sapphire/Emerald**, `item_registry.py` `icon_tile_id` -> `icon_key`, previously just reusing their own tile's block texture)
- [x] Asked to use everything usable, so pushed further with a `_colorize(base, color)` helper (`game/rendering/assets.py`, pure pygame `get_at`/`set_at` -- deliberately not numpy, which isn't a declared dependency in `requirements.txt`): recolors a real sheet icon by its own per-pixel luminosity rather than a flat alpha wash, so **one real shape stands in for several tinted tiers** while keeping its actual shading/linework, the same role `_build_procedural_icons`'s flat-fill silhouettes used to play. Applied to: the 5 **pickaxe** tiers (`_PICKAXE_BASE_CELL`), the 6 **bars** (`_INGOT_BASE_CELL`), the 13 **armor** pieces across wood/iron/steel (`_ARMOR_BASE_CELLS`, 4 slots x 3 tiers) plus a standalone **Arcane Helm**, **Iron Rod** (recolored off the same shape as **Twig Rod**) and the **Arcane Staff**
- [x] Also gave real icons to three items that previously had **no icon at all** (flat category swatch): **Slime Core Idol** (a glowing green orb), **Slime King's Core** (a diamond, distinct from the ore gems), and a bespoke gold/red-tinted paladin-helm recolor for the **Crown of the Slime King** (kept visually distinct from the plain tiered helmets, since it's the best head armor in the game, above even the Arcane Helm)
- [x] Caught and fixed a mid-pass mapping mistake: the cell first assigned to Grapple Hook was actually a plain curled wooden staff (looked hook-like at thumbnail size), and the cell assigned to Twig Rod's base was actually a blue-gemmed torch, not a plain rod. Re-verified every reused cell against zoomed, column-labeled crops before finalizing -- Grapple Hook now uses a real bent-fishing-hook icon from the sheet's fishing-tackle row, Twig Rod the plain wooden staff
- [x] Removed every procedural vector-icon block the sheet (directly or via `_colorize`) now permanently shadows -- only the **Arrow** icon is still hand-drawn vector, since the sheet has no standalone arrow/quiver icon (only bow/crossbow/sling weapons, no ammo)
- [x] Deliberately left alone: all placeable blocks/decorations (`places_tile_id` -- the inventory icon should show exactly what gets placed, not an unrelated sheet icon), Torch/Wood (already tile-texture icons appropriate to their nature), and the 8 fruit items (`_STATIC_ICON_SOURCES`, already dedicated real sprite art, not a gap)
- [x] Headless tests: full suite green (546); `test_phase2_items.py`/`test_phase6_biomes.py`'s ore-icon regression checks updated from asserting `icon_tile_id` to the new `icon_key`. Verified visually via rendered inventory-slot-sized contact sheets for every swapped/recolored icon (11 direct crops, 5 pickaxe tiers, 6 bars, 13 armor pieces + Arcane Helm, the Crown, both summon rods, the Arcane Staff)

Known gaps, intentionally deferred:
- **Arrow** stays a hand-drawn vector icon -- no matching ammo/arrow art found in the sheet
- Most of the sheet's ~350 icons are still unused (potions, food, tools like axe/hammer/shovel, books, instruments, ...) -- none of those items currently lack a reasonable icon (fruits have real sprites, blocks reuse tile textures appropriately), so there was nothing left to usefully swap them onto without inventing new items

## New terrain tileset, layered parallax background, sprite-based trees (user-provided: "coloquei na pasta assets novos tiles, background e arvores, troque tudo pra mim"): DONE
- [x] **Terrain**: the user dropped a dedicated `assets/Tiles/Tileset Outside.png` (grass-topped dirt + plain dirt, 32px autotile cells) and `Tileset Inside.png` (cave stone) into the project, replacing the old mixed-theme `assets/Terrain/Terrain.png` this game only ever used 3 cells of anyway (see README "Art reuse"). `GRASS_ID`/`DIRT_ID` now source from Outside, `STONE_ID` from Inside (`_OUTSIDE_TILESET_CELLS`/`_INSIDE_TILESET_CELLS` in `game/rendering/assets.py`) -- one fixed representative cell per tile id, same "no neighbor-awareness" approach the old single-cell system already used, not the full 9-slice autotiling both sheets technically have the pieces for (out of scope for a straight swap)
- [x] Every tile that derives its texture by tinting grass/dirt/stone (ore, bedrock, Workbench, Wood Plank, the legacy Tree Trunk/Leaves, every biome's sand/snow/jungle/desert ground and deep-stone variant, Furnace, Trap Ice, ...) picked up the new art for free through that same tinting pipeline (`_tinted`/`_with_speckles`, both size-agnostic). The handful of derived textures that draw decorative lines/rects at *hardcoded pixel coordinates* (Workbench's tabletop band + leg lines, the legacy Tree Trunk's bark lines, Wood Plank's seam line, Furnace's mouth/ember rect + line) had those coordinates doubled to match the new tileset's native 32px cells (was tuned against the old 16px `Terrain.png` cells) -- everything else (`_tinted`/`_with_speckles`) needed no change since they scale to whatever base they're given
- [x] **Background**: replaced the single flat tiled color swatch (`assets/Background/Blue.png`, `BACKGROUND_PARALLAX`) with the user's 4-layer parallax pack (`assets/Backgrounds/`: Sky, Clouds, Rock Mountains, Grass Mountains, each 320x320 and horizontally seamless) -- back to front, each its own scroll speed (`settings.BACKGROUND_LAYER_PARALLAX`: sky barely moves, each silhouette layer a bit faster than the one behind it) for an actual depth cue instead of a flat scrolling color. Each layer is tiled horizontally only and pre-scaled to the window's height once at load, not per frame -- these are a fixed sky+skyline composition (transparent top revealing the layer behind, opaque silhouette at the bottom), so vertical tiling would incorrectly stack mountain shapes on top of each other going up into open sky
- [x] **Trees, and a new "falls all at once" chop system**: the user supplied `assets/Tiles/Trees.png` (2 full 64x96 tree sprites, real art instead of stacked tile blocks) and explicitly asked for a different chopping system to match: "faça um sistema diferente pra cortar ela, pra ela cair de uma vez". Replaced the old 5-tile trunk+canopy stack (3 trunk tiles + 2 leaf tiles, each broken separately) with a single new `TREE_ID` tile at the trunk's base (`tile_registry.py`) -- world generation (`world_generator._place_forest_decoration`) now places just that one tile per tree, and `Renderer._draw_world` special-cases `TREE_ID` to draw a real sprite (one of the 2 variants, chosen deterministically from the tile's own column) spanning well beyond its single tile, in a second pass after every ordinary tile so its overlap into neighboring columns never gets clipped by a later-drawn neighbor. One continuous mining action now fells the whole tree in one break instead of five separate ones
- [x] `TileDef` gained `break_quantity: int = 1` (`game/world/tile.py`) so `TREE_ID` can still grant a whole tree's worth of wood (`break_quantity=5`, matching the old system's 5-tiles-worth-of-wood total) from that one break, added on top of the usual fortune-chance +1 bonus (`InputHandler.update_continuous`, reading the tile id *before* `Player.try_mine` breaks it). Every other tile keeps the implicit default of 1, unchanged
- [x] `TREE_TRUNK_ID`/`TREE_LEAVES_ID` (and their now-legacy-only textures) were **not** deleted -- kept registered so an existing save's chunk diff can still resolve a leftover trunk/leaves tile id without a `KeyError`, they're just never placed by world generation anymore
- [x] Headless tests: full suite green (548). `tests/test_phase3_crafting.py`'s tree tests rewritten for the new single-tile shape (`test_tree_spawns_and_is_choppable`, `test_chopping_a_tree_fells_it_all_at_once` -- the old canopy-adjacency test no longer applies, there's no separate canopy tile to check for); `test_phase6_biomes.py`'s "no trees in the desert" check updated to `TREE_ID`. Verified visually via real `GameApp.step()` renders (normal and 1.8x zoom) showing the parallax mountains, new grass/dirt/stone art, and a properly-anchored tree sprite, plus an end-to-end mining simulation through the real `InputHandler` mouse path confirming a chopped tree's tile clears in one break and grants exactly 5 wood

Known gaps, intentionally deferred:
- No neighbor-aware autotiling -- both new tilesets include the edge/corner pieces for it, but wiring that up is a separate, much larger feature than "swap the art" (see README "Art reuse")
- Clicking to chop a tree now targets only its one base tile, not anywhere on the wider visual canopy (the old 5-tile system let you click any part of the tree) -- a deliberate consequence of "one tile, real sprite," matching how similar games (e.g. Terraria) gate chopping on the trunk rather than the leaves
- Sprite variant selection is a fixed hash of the tile's column, not stored per-tree -- fine since it only needs to be *stable*, not meaningful, but means regenerating the same seed always picks the same variant per tree (already true of everything else in world generation)

## Crafting/Furnace screen split + a real furnace queue (user-requested: "melhore o sistema de craft e fornalha, quando eu clicar em ambos ai sim deve abrir a tela deles, cada um separado, quero funace queue... melhore o layout... crie uma especie de filtros... mostrar receitas por raridade"): DONE
- [x] Crafting and the Furnace are two separate screens now instead of one "press C" screen showing both crafting recipes and smelting recipes together. Left-clicking a placed Workbench opens Crafting, left-clicking a placed Furnace opens the Furnace screen (`InputHandler._try_open_station_screen`, checking the exact clicked+in-range tile id) -- skipped while a mining tool is selected, so holding a pickaxe out to mine/relocate a station still works. `C` remains a quick keyboard shortcut for Crafting only; the Furnace has no keybind, only the click, since every furnace recipe needs one nearby anyway. Both close on **Esc** (which now closes whichever panel/menu is open, in priority order, before falling back to pausing)
- [x] **Furnace queue** (`game/crafting/furnace_system.py`): each furnace tile gets its own 2-slot input hopper (`FurnaceManager.input_at`, fuel then ore) instead of a smelt recipe consuming straight from the player's inventory on click. The Furnace screen's bag grid auto-routes a clicked item into whichever slot accepts it (`smelt_registry.all_fuel_item_ids()`/`all_ore_item_ids()`); `FurnaceManager.update(dt)` auto-restarts the next batch (`_try_autostart`) the instant the previous `FurnaceJob` finishes, as long as the hopper still holds enough -- deposit a whole stack of ore + coal once and it works through it on its own, bars granted straight to the inventory as each batch completes, exactly like before. The original instant one-shot `start_smelt` (consumes directly from the player's inventory) is untouched and still available as a lower-level building block, just unused by the new screen
- [x] Crafting screen redesigned again, bigger and less cluttered (user feedback: "está muito confuso"): grid widened from 5 to 7 columns, taller viewport (`CRAFTING_VIEWPORT_HEIGHT` 460 -> 560), wider details panel (210 -> 260px). A new filter bar between the title and the grid: five rarity tabs (All/Common/Uncommon/Rare/Epic, filtering the *result item's* `ItemRarity`) plus a "Craftable now" toggle that hides anything the player can't click right now (missing ingredients or the wrong station) -- both are session-only `GameApp` state (`crafting_filter_rarity`/`crafting_filter_craftable_only`), threaded through every crafting-grid helper as optional trailing params so old unfiltered call sites (including existing tests) needed no changes
- [x] A small "Click: Craft" / "Click: Furnace" floating world-space hint (`Renderer._draw_station_prompt`, same look as the existing chest prompt) appears above a nearby Workbench/Furnace whenever its screen isn't already open, so the new click-to-open interaction is actually discoverable
- [x] `save_system.py` persists each furnace's input hopper (`furnace_inputs`, alongside the existing `furnace_jobs`) so saving mid-queue doesn't silently drop whatever ore/fuel stack was still smelting
- [x] Headless tests (`tests/test_crafting_and_furnace.py`, new cases): deposit/withdraw fuel and ore (including rejecting a non-fuel/non-ore item and a mismatched item already in a slot), the queue auto-starting the instant both slots have enough, working through 3 full batches unattended from one deposit, staying idle on insufficient quantity, clicking a Workbench/Furnace opening the right screen (and only that screen), the mining-tool-selected skip, and the Furnace screen's bag-click deposit routing -- all through the real `GameApp`/`InputHandler`, not just `FurnaceManager` in isolation. Plus `tests/test_phase7_save_load.py`'s new input-hopper round-trip test, and `tests/test_phase3_crafting.py` updated for the filter-bar height in the panel-height ceiling and for `_recipe_sections` no longer including a smelting section at all
- [x] Verified visually via the established headless-SDL screenshot method (real `GameApp.step()` renders, not just unit tests): the redesigned Crafting screen with its filter bar and 7-column grid, the "Craftable now" filter actually narrowing the grid, and the Furnace screen mid-queue (fuel/ore slots filled, a live "Smelting Iron Bar, Xs left" progress bar, and the bag grid below)

Known gaps, intentionally deferred:
- The input hopper is exactly 2 slots (one fuel type + one ore type queued at a time) -- no multi-recipe batching in one furnace, and Coal is still the only registered fuel so the "fuel doesn't match this ore's recipe" branch in `_try_autostart` is unreachable with today's data (future-proofing for a second fuel type, not a currently-triggerable state)
- No explicit "cancel smelting" action -- withdrawing the ore/fuel slot mid-batch only removes what's still sitting in the hopper, an already-started `FurnaceJob`'s in-flight batch always finishes
- Crafting filter state (rarity tab, "Craftable now") resets to defaults on a fresh app boot / after Load -- deliberately session-only, not worth persisting to the save file
- Still only one crafting recipe can be in progress at a time player-wide (`Player.craft_job`, unchanged) -- placing more furnaces parallelizes smelting, but there's no equivalent for hand/workbench crafts

## Itemization overhaul: full weapon/armor progression, a crit + move-speed stat engine, and a new endgame tier (user-requested: "melhore todo o sistema de itemização, builds por classe, status de cada item, crie novos itens late games, novas armas etc, deixe 3 agentes focados nisso"): DONE
Run as 3 parallel agents in isolated git worktrees (user explicitly asked for 3), each owning a disjoint slice of files to keep merges mechanical -- see each subsection below for what landed. Integrated by squash-merging each branch in turn, resolving the handful of expected append-conflicts (three agents independently adding new sections near the same spots in `item_registry.py`/`recipe_registry.py`/`summon_registry.py`, and all three separately patching the same pre-existing test that hard-coded the summon-id set), then a full `python -m pytest -q` pass. **628 tests passing** after integration (up from 548 before this pass + the Crafting/Furnace pass above).

**Before this pass**, the weapon roster was almost empty despite a full pickaxe/armor material ladder existing (Wood->Stone->Iron->Steel->Arcane): only `wood_sword` (melee) and `wood_bow` (ranged) existed below the endgame `arcane_staff`, no Iron/Steel sword or bow at all, the Summoner had only 2 rod tiers, the Arcane armor set was helm-only (no body/legs/boots), there was exactly one accessory in the whole game (Grapple Hook), and `ItemDef` had no crit or move-speed stat fields at all -- "status de cada item" (each item's stats) meant damage/defense/durability/rarity and nothing else.

- [x] **Mid-tier weapons** (Agent A -- `game/items/item_registry.py`, `game/crafting/recipe_registry.py`, `game/entities/summon_registry.py`): filled the Iron/Steel gap -- **Iron Sword** (dmg 14, UNCOMMON) / **Steel Sword** (dmg 20, RARE), **Iron Bow** (dmg 11) / **Steel Bow** (dmg 16), a **Steel Rod** summon item casting a new **Steel Colossus** (dmg 21, between Iron Guardian's 14 and the new Arcane tier below), and two passive **ACCESSORY** items giving each class an early build identity for free (no new engine code -- `Equipment.total_defense()`/`total_light_emit()` already sum every slot including accessory): **Warrior's Charm** (defense 6) and **Summoner's Trinket** (defense 3 + light_emit 6)
- [x] **A real item-stat engine: crit chance + move speed** (Agent B -- owned the only engine-code changes in this pass): `ItemDef` gained `crit_chance: float = 0.0`, `crit_damage_mult: float = 1.5`, `move_speed_bonus: float = 0.0`. Wired into `game/combat/combat_system.py` (`_try_melee_attack`/`_try_ranged_attack` roll a crit after the existing Attack/Magic multiplier; `resolve_summon_attacks` also rolls crit off the player's equipped rod, so the Summoner class benefits from the same system, not just Warrior), `game/inventory/equipment.py` (new `total_move_speed_bonus()`, same sum-every-slot pattern as `total_defense`), `game/entities/player.py` (`move_left`/`move_right` add the bonus on top of `PLAYER_MOVE_SPEED`), and the item tooltip (`Renderer._item_tooltip_stat_lines` gained `Crit Chance: X%` / `Move Speed: +X` lines)
- [x] **Arcane tier completed** (Agent B): `arcane_body` (defense 16), `arcane_greaves` (11), `arcane_boots` (7) fill the previously helm-only set; a new **Arcane Sword** (dmg 28, `crit_chance=0.15`) and **Arcane Rod** (casts a new **Arcane Familiar** summon, dmg 27) round out every weapon_class at this tier; the existing `arcane_staff` picked up `crit_chance=0.1` too, so the endgame magic weapon isn't left out of the new system. Two new move-speed accessories: **Swift Anklet** (+1.0) and **Arcane Anklet** (+1.5, EPIC)
- [x] **A brand-new post-Arcane endgame tier: Voidsteel** (Agent C -- the "novos itens late game" ask): a new universal (not biome-locked), very-deep-only ore (`VOID_ORE_ID`, `game/world/tile_registry.py`; `VOID_ORE_MIN_DEPTH_BELOW_SURFACE`=45, `VOID_ORE_SPAWN_CHANCE`=0.0015, roughly 1/8th of Iron's roll chance -- see `game/world/world_generator.py`), smelted (`SMELT_TIME_VOIDSTEEL_S`=14.0, the slowest smelt in the game) into a **Voidsteel Bar**, which crafts a full set above everything else that exists: **Voidsteel Sword** (dmg 38), a complete armor set (helmet 19 / armor 21 / greaves 14.5 / boots 10.5 defense -- all above the previous ceiling, `slime_king_crown`'s 16), a **Voidstone Amulet** accessory (defense 8 + light_emit 13), and a **Voidsteel Rod** casting the new **Void Wraith** (dmg 34, the strongest summon in the game)
- [x] **Slime King's loot line expanded** (Agent C): 2 new recipes consuming the existing boss-exclusive `slime_king_core` drop alongside the existing Crown -- **Slime King's Fang** (weapon, dmg 30) and **Slime King's Bulwark** (body armor, defense 18) -- a farmable side-grade to the Voidsteel tier, each costing another full boss kill (`slime_king_core x1 + steel_bar x3`, same cost shape as the Crown), not a strictly-required upgrade path
- [x] Headless tests: `tests/test_itemization_weapons.py`, `tests/test_itemization_stats.py`, `tests/test_itemization_endgame.py` (new, one per agent) plus updates to `tests/test_summoner_class.py` (the summon-id-set assertion, reconciled to a forward-compatible subset check across all three branches' independent fixes) and `tests/test_phase6_biomes.py` (widened to allow the new universal Voidstone ore in every biome's underground tile set). Full suite green at 628.

Known gaps, intentionally deferred:
- Crit doesn't have its own visual/audio cue (no distinct "crit!" popup color or sound) -- a crit hit currently reads the same as a normal one except for the bigger number
- `move_speed_bonus` items only affect horizontal ground movement (`move_left`/`move_right`) -- jump velocity, grapple pull speed, etc. are untouched
- No true "build" mechanic beyond gear choice + the existing skill tree -- "builds por classe" landed as *more distinct gear per class* (class-flavored accessories, a Summoner rod at every tier a Warrior gets a weapon), not a new passive/talent system layered on top
- The 3-agent split means numeric balance across tiers was calibrated by written guidance (damage/defense target ranges handed to each agent in advance) rather than actual playtesting -- the progression *reads* coherent (each tier strictly exceeds the last) but hasn't been played through end-to-end
- Voidsteel's ~1/8th-of-Iron spawn rarity is a design guess, not tuned against real mining-time data

### Follow-up (user feedback: "coloque as sprites nos itens novos... senti falta em todas as receitas os status de cada item... e nos itens do inventario tbm"): DONE
- [x] **Real sprites for every new item** (`game/rendering/assets.py`): extended the existing "one real shape from the user-supplied icon sheet, recolored per tier with `_colorize`" technique (same one pickaxes/bars/armor already used) instead of hunting for or drawing new art. New tier color `_TIER_COLORS["voidsteel"]` (a deeper, more saturated violet-black than arcane's purple so the two read as distinct tiers, not just light/dark copies of the same color). New base-cell constants: `_SWORD_BASE_CELL`/`_BOW_BASE_CELL` (the same shapes as `wood_sword`/`wood_bow`'s own natural-color cells, recolored for Iron/Steel/Arcane/Voidsteel), the existing rod cell reused for the Steel/Arcane/Voidsteel rods, the existing `_ARMOR_BASE_CELLS` reused for the rest of the Arcane set and the full Voidsteel armor set, `_VOID_ORE_CELL` (a plain raw-stone shape, distinct from the coal/gem ore-pile icons already in use) for Voidstone, and three new jewelry base cells (`_ACCESSORY_RING_CELL`/`_AMULET_CELL`/`_BRACELET_CELL`, found by generating a labeled, zoomed contact-sheet crop of the icon sheet and reading off row/col by eye -- same verification method the README already documents for past icon passes) for the 5 new accessories. Slime King's Fang/Bulwark reuse the existing Crown's gold/red tint so the three read as one themed set. Every new item's `icon_key` (several were already pre-wired by Agent B/C in the itemization pass, expecting exactly this follow-up) now resolves to real art instead of falling back to the flat category swatch.
- [x] **Recipe details panel now shows the crafted item's own stats** (`Renderer._draw_recipe_details`, in `game/rendering/renderer.py`): previously showed name/station/ingredients/status but never what the *result* item actually does -- reused the existing `_item_tooltip_stat_lines` helper (same block the inventory tooltip already built) to add a damage/defense/crit/move-speed/durability/value block between the header and the ingredient list. This applies to every recipe, not just the new items -- the gap predated this pass entirely.
- [x] **The hotbar now shows a stat tooltip on hover** (`Renderer._draw_hotbar`): the hotbar is the one place items are visible during normal play with no menu open, and it never called `_draw_item_tooltip` at all (unlike the Inventory/Chest/Furnace/NPC-shop screens, which already did) -- added the same hover-and-show-tooltip pattern used everywhere else.
- [x] Verified visually via the established headless-SDL screenshot method: the Crafting screen's Weapons section showing every new tinted sword/bow/rod icon, the Iron Sword and Voidsteel Sword recipe-detail panels both showing their stat block above the ingredient list, the Character screen showing every new armor/accessory icon (confirmed by sampling average pixel color that Slime King's Bulwark shares the Crown's gold hue, since the two look close at 48px and needed a numeric check, not just an eyeball one), and the hotbar's hover tooltip firing correctly (mocking `pygame.mouse.get_pos` was required for this -- SDL's dummy video driver used by every headless script/test in this project does not honor `pygame.mouse.set_pos`)
- [x] **Summoner rods now show the summon they actually cast, not "Damage: 0"** (user feedback, same follow-up: "os status do summoner nas rods nao aparece, quantidades de summon, dano do summon já com base no magic level"): a rod's own `ItemDef.damage`/`is_ranged` always default to 0.0/False -- the real numbers live entirely on the `SummonDef` it casts (`summon_registry.py`) -- so every rod's tooltip used to hit the generic weapon branch and show a meaningless "Damage: 0" / "Melee" pair. New `Renderer._summon_rod_stat_lines(item_def, player)` detects `weapon_class == "summon"` and instead shows the cast summon's name, base damage, **effective damage live-scaled by the player's current Magic level** (`summon_def.damage * player.skills.magic_damage_multiplier()`, the same formula `combat_system.resolve_summon_attacks` actually uses to deal damage, so the tooltip never lies), crit chance (if the rod has one), attack speed, attack range and the summon's own move speed. Required threading an optional `player` parameter through the whole tooltip call chain (`_item_tooltip_stat_lines`, `_draw_item_tooltip`, and every one of its 6 call sites: hotbar, inventory bag, chest, NPC shop, furnace screen, and the recipe-details stat block added in the entry above) since only the Magic-multiplier line needs it -- every other stat still renders fine with `player=None`. New `tests/test_summon_rod_tooltip.py` (8 tests): the summon-block routing (regression-guards the exact "Damage: 0"/"Melee" bug), the effective-damage number rising after gaining Magic XP, graceful behavior with no player given, ordinary (non-rod) weapons untouched, and every registered rod resolving a real `SummonDef`.

## Day-based enemy difficulty + always-on name/HP bars (user-requested: "vamos fazer a progressao do jogo, cada dia que passa os mobs ficam mais fortes, e coloque para mostrar a vida deles e o nome em cima do sprite"): DONE
- [x] **Day-based difficulty scaling** -- new `game/entities/difficulty.py`, two pure functions of `WorldClock.day_count`: `health_multiplier`/`damage_multiplier`, each `1.0 + (day_count - 1) * PCT_PER_DAY` up to a cap (`ENEMY_HEALTH_PCT_PER_DAY`=0.08, `ENEMY_DAMAGE_PCT_PER_DAY`=0.05, `ENEMY_DIFFICULTY_MAX_DAY`=30 in `settings.py` -- by day 30 a mob has +232% health / +145% damage over day 1, then flatlines so a very long playthrough doesn't scale into absurd numbers). `Enemy.__init__` gained `health_multiplier`/`damage_multiplier` params (default 1.0, so every old call site is unaffected) and now stores instance-level `max_health`/`contact_damage` separate from the shared, frozen `EnemyDef` -- two Slimes spawned on different days can now genuinely differ in toughness, since the def itself is never mutated. `Boss` forwards the same two params to `Enemy.__init__`. Applied at spawn time only (not retroactively) in `EnemySpawner._try_spawn` (now takes a `day_count` param, defaulting to 1), `GameApp.try_summon_boss`, and both debug spawn tools (`debug_spawn_all_bosses`/`debug_spawn_all_enemies`) -- all four read `self.world_clock.day_count`. The Slime King's enrage-minions (`boss_ai._spawn_minions`) derive their multiplier from the boss's own already-scaled stats (`boss.max_health / boss.enemy_def.max_health`) rather than needing `day_count` threaded all the way down into `boss_ai.py`, so they're never a soft spot on a harder day.
- [x] Fixed two call sites that used to read `enemy.enemy_def.max_health`/`contact_damage` directly (the shared, unscaled definition) instead of the new per-instance scaled value: `combat_system.resolve_contact_damage` (would have dealt the *unscaled* contact damage forever, silently ignoring the whole feature) and `boss_ai.update`'s phase-ratio calculation (would have made a scaled boss enter later phases too early, since its ratio would read artificially low against the bigger unscaled denominator).
- [x] **Every non-boss enemy now shows its name and an HP bar floating above it, always** (`Renderer._draw_entity_health_bar`, in `game/rendering/renderer.py`) -- previously this only fired for enemies with a real sprite (`enemy_animations`, not the flat-vector-shape ones like Slime/Crawler/Duskwing/Scorpion) and only while damaged (`health < max_health`), so most mobs had no name or bar at all until hit, and never before that. Now called for every alive non-boss enemy regardless of render style (sprite or vector shape), unconditionally, so a player can size up an unfamiliar (and possibly difficulty-scaled-tougher) mob before engaging. Bosses are explicitly skipped (`ai_type == AIType.BOSS` early-return) since they already get a persistent top-center name+phase+bar (`_draw_boss_bar`) -- a second one floating over the sprite would be redundant.
- [x] Headless tests (`tests/test_enemy_difficulty_and_hud.py`, 16 new): the multiplier math (baseline at day 1, per-day growth rate, monotonic, caps at `ENEMY_DIFFICULTY_MAX_DAY`, never drops below baseline on a bad input), `Enemy`/`Boss` correctly scaling their own instance stats without mutating the shared `EnemyDef`, `boss_ai`'s phase-ratio regression guard, minions inheriting the boss's scaling, `EnemySpawner._try_spawn`/`.update` actually threading `day_count` through (and defaulting to 1, matching old behavior, when not given), and the health-bar/name draw call: something is actually drawn even at full health (regression guard for the old early-return), bosses are a true no-op, and the bar's fill ratio reflects current health.
- [x] Verified visually via the established headless-SDL screenshot method: `world_clock.day_count` set to 15, a Slime/Crawler/Duskwing spawned side by side (one vector-shape mob, two sprite mobs) all showing their name + HP bar above the sprite, the damaged Slime's bar showing a partial red fill next to its floating damage popup. Cross-checked the actual scaling numbers in isolation too (day 1/2/5/10/30/60 for a Slime): health/damage climb smoothly through day 30 then hold flat at day 60, confirming the cap works.

Known gaps, intentionally deferred:
- Only contact damage and max health scale -- a boss's special attacks (Slime Lob damage, stomp damage) stay at their own hand-tuned constants (`SLIME_KING_LOB_DAMAGE`/`SLIME_KING_STOMP_DAMAGE`), not multiplied by the day-based scaling, so a very-late-game Slime King hits harder on contact but not on its ranged/stomp attacks
- No visual/HUD indicator of the *current* difficulty multiplier anywhere (e.g. no "Day 15 -- enemies are 32% stronger" readout) -- a player only sees the effect (tougher mobs, bigger numbers), not the cause spelled out
- Enemy move_speed doesn't scale with day -- only health and contact damage do
- The name+HP-bar text has no outline/stroke, just a small semi-transparent backing rect for contrast -- fine at the tested zoom levels, may be harder to read over a very bright background tile

## Biome exclusive mobs (3-agent content pass, Agent A): DONE
- [x] **Frost Hopper** (`frost_hopper`) -- Snow exclusive, `AIType.HOP`, icy-tinted Mini_Slime sheets (own loader, does not call `load_slime_animations`), drops `frost_shard`
- [x] **Swamp Mosquito** (`swamp_mosquito`) -- Jungle exclusive, `AIType.FLY`, `assets/Enemies/mosquito.png` (48px flap on row 0), drops `mosquito_wing`
- [x] Drop crafts so the materials aren't dead ends: `frost_arrows` (1 frost_shard + 1 wood -> 8 arrows, anywhere) and `wing_charm` (Workbench: 4 mosquito_wing + 2 slime_gel, accessory `light_emit` 4)
- [x] Headless tests: `tests/test_new_mobs.py` plus Snow/Jungle exclusive gates in `test_phase6_biomes.py` and the Phase 4 enemy-id set widened for the two new ids

## Dead-end materials + consumables (3-agent content pass, Agent B): DONE
- [x] **Healing Potion** (`healing_potion`) -- CONSUMABLE, heal 40, Uncommon, craft anywhere from 2 apple + 1 slime_gel; Merchant sells it at 14 (value 8)
- [x] **Cactus Jerkin** (`cactus_jerkin`) -- body armor, defense 8 (between wood 6 and iron 10); Workbench 8 cactus_fiber + 4 wood; Blacksmith sells it above value
- [x] **Feather Charm** (`feather_charm`) -- accessory, +0.5 move speed and 1 defense, Common; anywhere from 6 feather + 2 wood (duskwing drop sink)
- [x] **Gem-bar rings** -- `topaz_ring` (5% crit), `sapphire_ring` (defense 4 + light 8), `emerald_ring` (+0.8 move speed); each 1 matching gem bar + 1 iron_bar at a Workbench, so those bars aren't Arcane-Bar-only
- [x] Icons from the unused sheet: potion flask cell (9, 0), cactus-green tint of the body-armor cell, feather cell reused for the charm, `_ACCESSORY_RING_CELL` colorized per gem-bar color
- [x] Headless tests: `tests/test_new_content_items.py`

## Missing sprites (3-agent content pass, Agent C): DONE
- [x] **Crawler** -- `assets/Enemies/Eyeball.png` (288x96, 48px cells: idle row 0 / chase row 1 cols 0-3 / red-tint hit), added to `load_enemy_animations`
- [x] **NPCs** -- Guide/Merchant/Blacksmith reuse Mask Dude / Pink Man / Virtual Guy idle frames (`NpcDef.sprite_character_id`); `_draw_npcs` blits them facing the player with lighting + nameplate
- [x] **Summons** -- Twig/Steel/Arcane/Void recolor the Iron Guardian flying-head flap via `_colorize` (green / blue-gray / purple / dark purple)
- [x] **Door + Bed** -- procedural wood-door (closed + open gap) and bed (mattress + pillow) on a dirt/plank-tinted base, same pattern as Workbench
- [x] Headless tests: `tests/test_missing_sprites.py` plus crawler assertions in `test_phase4_combat.py`

## Cross-cutting (ongoing, not a phase)
- [x] Background music (see "Background music" above) -- looping `Music/background.mp3` via `pygame.mixer.music`
- [x] Sound effects (see "Sound effects" above) and a real audio settings screen (see "Settings screen overhaul" above) -- not blocking on final audio assets
- [x] Debug tools beyond F3 overlay (see "Debug tools" above): give-item (via unlock-all-recipes + restock), teleport, spawn-enemy/spawn-boss -- regen-world still deferred, see that section's known gaps
- [x] Configurable input bindings (Pause -> Settings -> Controls): click a key chip, press a new key; swaps on collision; Esc/1-9/F-keys reserved; persisted in `saves/settings.json` with volume. Headless tests in `tests/test_key_bindings.py`
